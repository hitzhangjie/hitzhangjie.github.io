<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>C/C++ on</title><link>/categories/c/c++/</link><description>Recent content in C/C++ on</description><generator>Hugo -- gohugo.io</generator><language>en-US</language><lastBuildDate>Fri, 24 Jul 2020 19:46:11 +0800</lastBuildDate><atom:link href="/categories/c/c++/index.xml" rel="self" type="application/rss+xml"/><item><title>你不认识的cc++ volatile</title><link>/blog/2019-01-07-%E4%BD%A0%E4%B8%8D%E8%AE%A4%E8%AF%86%E7%9A%84cc-volatile/</link><pubDate>Fri, 24 Jul 2020 19:46:11 +0800</pubDate><guid>/blog/2019-01-07-%E4%BD%A0%E4%B8%8D%E8%AE%A4%E8%AF%86%E7%9A%84cc-volatile/</guid><description>&lt;style&gt;
img {
width: 680px;
}
&lt;/style&gt;
&lt;h2 id="1-令人困惑的volatile"&gt;1. 令人困惑的volatile &lt;a href="#1-%e4%bb%a4%e4%ba%ba%e5%9b%b0%e6%83%91%e7%9a%84volatile" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;volatile字面意思是“&lt;strong&gt;不稳定的、易变的&lt;/strong&gt;”，不少编程语言中存在volatile关键字，也有共同之处，如“&lt;strong&gt;表示程序执行期间数据可能会被外部操作修改&lt;/strong&gt;”，如被外设修改或者被其他线程修改等。这只是字面上给我们的一般性认识，然而具体到不同的编程语言中volatile的语义可能相差甚远。&lt;/p&gt;
&lt;p&gt;很多人以为自己精通CC++，但是被问起volatile的时候却无法清晰、果断地表明态度，那只能说明还是处在“从入门到精通”的路上，如果了解一门语言常见特性的使用、能够写健壮高效的程序就算精通的话，那实在是太藐视“大师”的存在了。从一个volatile关键字折射出了对CC++标准、编译器、操作系统、处理器、MMU各个方面的掌握程度。&lt;/p&gt;
&lt;p&gt;几十年的发展，很多开发者因为自己的偏见、误解，或者对某些语言特性（如Java中的volatile语义）的根深蒂固的认识，赋予了CC++ volatile本不属于它的能力，自己却浑然不知自己犯了多大的一个错误。&lt;/p&gt;
&lt;p&gt;我曾经以为CC++中volatile可以保证线程可见性，因为Java中是这样的，直到后来阅读Linux内核看到Linus Torvards的一篇文档，他强调了volatile可能带来的坏处“任何使用volatile的地方，都可能潜藏了一个bug”，我为他的“危言耸听”感到吃惊，所以我当时搜索了不少资料来求证CC++ volatile的能力，事后我认为CC++ volatile不能保证线程可见性。但是后来部门内一次分享，分享中提到了volatile来保证线程可见性，我当时心存疑虑，事后验证时犯了一个错误导致我错误地认为volatile可以保证线程可见性。直到我最近翻阅以前的笔记，翻到了几年前对volatile的疑虑……我决定深入研究下这个问题，以便能顺利入眠。&lt;/p&gt;
&lt;h2 id="2-从规范认识volatile"&gt;2. 从规范认识volatile &lt;a href="#2-%e4%bb%8e%e8%a7%84%e8%8c%83%e8%ae%a4%e8%af%86volatile" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;以常见的编程语言C、C++、Java为例，它们都有一个关键字volatile，但是对volatile的定义却并非完全相同。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;Java中对volatile的定义：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;8.3.1.4. &lt;code&gt;volatile&lt;/code&gt; Fields&lt;/p&gt;
&lt;p&gt;The Java programming language allows threads to access shared variables (&lt;a href="https://docs.oracle.com/javase/specs/jls/se7/html/jls-17.html#jls-17.1"&gt;§17.1&lt;/a&gt;). As a rule, to ensure that shared variables are consistently and reliably updated, a thread should ensure that it has exclusive use of such variables by obtaining a lock that, conventionally, enforces mutual exclusion for those shared variables.&lt;/p&gt;</description></item><item><title>数组与指针</title><link>/blog/%E6%95%B0%E7%BB%84%E4%B8%8E%E6%8C%87%E9%92%88/</link><pubDate>Wed, 13 Feb 2019 08:00:00 +0800</pubDate><guid>/blog/%E6%95%B0%E7%BB%84%E4%B8%8E%E6%8C%87%E9%92%88/</guid><description>&lt;h1 id="1-二维数组之行列指针"&gt;1 二维数组之行列指针 &lt;a href="#1-%e4%ba%8c%e7%bb%b4%e6%95%b0%e7%bb%84%e4%b9%8b%e8%a1%8c%e5%88%97%e6%8c%87%e9%92%88" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;pre&gt;&lt;code class="language-c"&gt;//数组名是数组首元素的地址，但是数组是一种类型，不能完全等同于指针，
//sizeof(指针)=操作系统虚拟内存位数对应的字节数，sizeof(a)=sizeof(int)*2*3=24字节
int a[2][3] = {1,2,3,4,5,6};
int size = sizeof(a);
//行指针, 这里的i之所以称为行指针，是因为它定位的是第i行这个子向量
*(a+i) 等于 a[i]
//列指针，这里的j之所以称为列指针，是因为它定位的是第j列这个列向量
*(*(a+i)+j) 等于 a[i][j]
//如果需要获取其中的值，需要再次解除引用才可以
*(a+i)+j 等于 &amp;amp;a[i][j]
&lt;/code&gt;&lt;/pre&gt;
&lt;h1 id="2-多维数组指针"&gt;2 多维数组指针 &lt;a href="#2-%e5%a4%9a%e7%bb%b4%e6%95%b0%e7%bb%84%e6%8c%87%e9%92%88" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;p&gt;分析方法，与二维数组的分析没有什么差别！只是不建议那样使用！为什么要用呢？自寻烦恼嘛！
倒不是难以分析，而是写起来很啰嗦！&lt;/p&gt;
&lt;p&gt;多维数组，还是建议使用下标索引来使用要好一些！&lt;/p&gt;
&lt;h1 id="3-数组名称的特殊性"&gt;3 数组名称的特殊性 &lt;a href="#3-%e6%95%b0%e7%bb%84%e5%90%8d%e7%a7%b0%e7%9a%84%e7%89%b9%e6%ae%8a%e6%80%a7" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;p&gt;我们常说数组的名称就是一个指向数组本身的地址，或者说是一个指针，规范一点讲，数组名其实是数组第
1个元素的地址。如下所示：&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-c"&gt;int b[100]; //b 等于 &amp;amp;b[0];
int a[100][200]; //a 等于 &amp;amp;a[0];
int c[100][200][300]; //c 等于 &amp;amp;c[0];
&lt;/code&gt;&lt;/pre&gt;</description></item><item><title>a += (a++)+(++a) 的值到底是多少？</title><link>/blog/2019-02-13-a--a-a/</link><pubDate>Wed, 13 Feb 2019 00:00:00 +0800</pubDate><guid>/blog/2019-02-13-a--a-a/</guid><description>&lt;p&gt;前一阵子，在某厂笔试题（Java）里面，出现了这样一个考题：&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-c"&gt; int a = 0;
a += (a++) + (++a);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;上述两条语句执行之后，a的值是多少？&lt;/p&gt;
&lt;p&gt;这么简单的题目也需要考吗，这太简单了吧？知道怎么算是一回事， 明不明白是另一回事！而且不一定会算吧！&lt;/p&gt;
&lt;p&gt;【备注】
如果不是脑子有病，估计不会有人写这样的程序，那解释这道题有什么意义呢？确实，有很多更加值得折腾的东西，比如中间件、智能算法等等，相比起来，去钻研这种编程题太没意思了，出题人脑残还是咋滴，对此，我绝对同意。但是除了这种想法的人外，也确实有一部分人，想人所不想，能人所不能，你总不能说他们求知是个错误吧！今天，刚好有人问起，我就顺便回答了一下，然后整理了整理。
感兴趣的话就耐心往下看吧，也不长，估计应该能看懂。&lt;/p&gt;
&lt;p&gt;以前给红黑联盟里一帮小朋友解答过c里面前置增后置增的实现问题，没想到今天又在一个Java高级群里面碰到了群友问相同的问题，区别是java中的实现方式。其实，说白了，这些是编译原理层面的知识，也需要对内存模型有一定的理解，尤其要注意JMM。但是一般情况下我相信是没有几个人是愿意去翻gcc、javac的设计、实现的。那怎么办？
了解一下基本的内存模型，反汇编，可能还是比较实用的一种办法。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;1）c，编译器gcc&lt;/strong&gt;&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-c"&gt;int main(int argc, char *argv[]){
4004f6: 55 push %rbp
4004f7: 48 89 e5 mov %rsp,%rbp
4004fa: 89 7d ec mov %edi,-0x14(%rbp)
4004fd: 48 89 75 e0 mov %rsi,-0x20(%rbp)
int a = 0;
400501: c7 45 fc 00 00 00 00 movl $0x0,-0x4(%rbp) // 给a赋值0
a += (a++)+(++a);
400508: 8b 45 fc mov -0x4(%rbp),%eax // a++中初始装载变量a到eax
40050b: 8d 50 01 lea 0x1(%rax),%edx
40050e: 89 55 fc mov %edx,-0x4(%rbp) // lea将1+rax的值存储回变量a（变量a的值变为1），但是不修改rax的值。
// 使用的是lea，而非add，为啥？后置增，rax记录着a自增之前的旧值！
// 后置增，即使用后+1，这里的”使用后“，指的是执行整条语句之后吗？ 不是！
400511: 83 45 fc 01 addl $0x1,-0x4(%rbp) // ++a操作（变量a的值变为2）
400515: 8b 55 fc mov -0x4(%rbp),%edx
400518: 01 d0 add %edx,%eax // 执行 (a++) + (++a)操作，即0+2，并将结果暂存到eax
40051a: 01 45 fc add %eax,-0x4(%rbp) // 执行a+=...，即2+2并将结果存储到变量a（变量a的值变为4）
return 0;
40051d: b8 00 00 00 00 mov $0x0,%eax}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;看完上面的解释后，你应该能总结处这样的规律，(a++) + (++a)中，a++是先于++a执行的，即普通的表达式一般都是从左向右进行运算，另外你也应该记住lea指令的用法，在这里还不失巧妙。&lt;/p&gt;</description></item><item><title>协程编程模型：Coroutine-Switching</title><link>/blog/2017-04-26-coroutine-switching/</link><pubDate>Wed, 26 Apr 2017 16:23:49 +0800</pubDate><guid>/blog/2017-04-26-coroutine-switching/</guid><description>&lt;h3 id="1-协程coroutine"&gt;1. 协程Coroutine &lt;a href="#1-%e5%8d%8f%e7%a8%8bcoroutine" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;本文提供了一个模拟协程上下文切换过程的测试程序，基本思路是每当希望创建一个coroutine时，就在堆里申请一块内存，然后对内存进行整理，在其中保存预设的硬件上下文等信息（重点设置rbp、rsp、rip（callback）、rdi（callback-arg））。启动协程并进行切换时，通过__SwitchCoroutine__(cur,next)来完成cur向next的切换，切换过程中保存当前上下文信息到cur对应的堆内存中，并提取next对应的堆内存信息还原上下文完成切换。 测试程序中创建了4个coroutine，第1个coroutine只是为了用来启动其他3个，其他3个没有继续调度第一个，原因是第一个coroutine中rip保存的是main.c中__SwitchCoroutine__之后的指令地址，如果让第一个coroutine参与context-switch的话会使得进程结束执行。&lt;/p&gt;
&lt;p&gt;在看下面的程序之前，需要先了解栈帧的构成、栈帧创建之前caller的动作&amp;amp;创建之后callee的动作、函数返回之前callee的动作&amp;amp;函数返回之后caller的动作，之前看过一篇整理的很不错的文章以供参考C Function Call Conventions and the Stack。&lt;/p&gt;
&lt;p&gt;ps: 实际上直接使用linux下的ucontext_t实现会更简单，在我的介绍libmill的电子书中有详细介绍。&lt;/p&gt;
&lt;h4 id="11-协程coroutine声明"&gt;1.1. 协程coroutine声明 &lt;a href="#11-%e5%8d%8f%e7%a8%8bcoroutine%e5%a3%b0%e6%98%8e" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;file: coroutine.h&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-c"&gt;#include &amp;lt;stdint.h&amp;gt;
typedef int64_t (*EntryCallback)(void*);
//硬件上下文信息
struct stRegister
{
uint64_t rax;
uint64_t rbx;
uint64_t rcx;
uint64_t rdx;
uint64_t rsi;
uint64_t rdi;
uint64_t r8;
uint64_t r9;
uint64_t r10;
uint64_t r11;
uint64_t r12;
uint64_t r13;
uint64_t r14;
uint64_t r15;
uint64_t rbp;
uint64_t rsp;
uint64_t rip;
};
//协程上下文
struct stContext
{
struct stRegister cpu_register;
void *arg;
uint8_t *stack;
};
typedef struct stContext Coroutine;
//创建协程
Coroutine* CreateCoroutine(EntryCallback entry, void *arg);
//删除协程
void DeleteCoroutine(Coroutine *ptr);
//设置协程栈尺寸
void SetStackSize(uint32_t size);
//协程切换
void __SwitchCoroutine__(Coroutine *cur, const Coroutine *next);
&lt;/code&gt;&lt;/pre&gt;
&lt;h4 id="12-协程coroutine实现"&gt;1.2. 协程Coroutine实现 &lt;a href="#12-%e5%8d%8f%e7%a8%8bcoroutine%e5%ae%9e%e7%8e%b0" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h4&gt;
&lt;p&gt;file: coroutine.c&lt;/p&gt;</description></item><item><title>协程编程模型：jmp_buf助你更好理解"任务上下文"</title><link>/blog/2017-03-27-jmp_bufsetjmplongjmp/</link><pubDate>Mon, 27 Mar 2017 21:31:13 +0800</pubDate><guid>/blog/2017-03-27-jmp_bufsetjmplongjmp/</guid><description>&lt;p&gt;最近在看spp &amp;amp; libco源码，他们实现协程上下文切换的过程中，都或多或少借鉴了jmp_buf的设计，用以保存协程执行时的现场。协程切换的时候保存当前协程的现场，然后恢复待调度协程的现场。理解是很容易理解，但是总感觉还是有点浅尝辄止了，于是就抽点时间看了下jmp_buf、setjmp、longjmp相关的代码，大体理了下思路。&lt;/p&gt;
&lt;p&gt;学习整理了一下关于jmp_buf &amp;amp; setjmp &amp;amp; longjmp的内容。&lt;/p&gt;
&lt;p&gt;linux 4.0内核中jmp_buf这个结构体用于记录硬件上下文信息，可以用于函数内、函数外跳转，goto只能实现函数内跳转。先来看下这个结构体的定义吧，i386架构的处理器与x86_64架构的处理器，对应的jmp_buf结构体定义稍微有些不同，这个很容易理解，寄存器位宽、数量等都有些不同。&lt;/p&gt;
&lt;p&gt;i386架构：&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-c"&gt;// 处理器架构：i386
// - Linux/arch/x86/um/shared/sysdep/archsetjmp_32.h
struct __jmp_buf {
unsigned int __ebx; // 通用数据寄存器之一
unsigned int __esp; // 栈指针寄存器(进程栈空间由高地址向低地址方向增长)
unsigned int __ebp; // 基址指针寄存器(记录了当前栈帧的起始地址(进入一个函数后首先执行的便是push %ebp; mov %esp, %ebp))
unsigned int __esi; // 源变址寄存器
unsigned int __edi; // 目的编制寄存器
unsigned int __eip; // 指令指针寄存器(程序计数器PC=CS:IP,二者结合起来确定下一条待执行的机器指令地址)
};
typedef struct __jmp_buf jmp_buf[1];
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;x86_64架构：&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-c"&gt;// 处理器架构：x86_64
// - Linux/arch/x86/um/shared/sysdep/archsetjmp_64.h
struct __jmp_buf {
unsigned long __rbx; // 通用数据寄存器之一
unsigned long __rsp; // 栈指针寄存器
unsigned long __rbp; // 基址指针寄存器
unsigned long __r12;
unsigned long __r13;
unsigned long __r14;
unsigned long __r15;
unsigned long __rip;
};
typedef struct __jmp_buf jmp_buf[1];
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;但是呢，glibc里面重新定义了这个类型，这里面还对信号掩码进行了考虑。&lt;/p&gt;</description></item></channel></rss>