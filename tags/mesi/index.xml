<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>MESI on</title><link>/tags/mesi/</link><description>Recent content in MESI on</description><generator>Hugo -- gohugo.io</generator><language>en-US</language><lastBuildDate>Thu, 16 Jul 2026 20:10:00 +0800</lastBuildDate><atom:link href="/tags/mesi/index.xml" rel="self" type="application/rss+xml"/><item><title>写屏障与读屏障：Store Buffer 与 Invalidate Queue 的双重挑战</title><link>/blog/2026-07-16-write-barrier-and-read-barrier/</link><pubDate>Thu, 16 Jul 2026 20:10:00 +0800</pubDate><guid>/blog/2026-07-16-write-barrier-and-read-barrier/</guid><description>&lt;p&gt;多核 CPU 中，一个核心的写操作什么时候能被另一个核心看到？&lt;a href="2026-07-16-store-buffer-and-memory-barrier.md"&gt;上一篇文章&lt;/a&gt; 梳理了 Store Buffer 在这条链路中的作用。但光有 Store Buffer 还不够——另一个同样关键的硬件机制 &lt;strong&gt;Invalidate Queue&lt;/strong&gt; 在文章中没有展开。本文把这两个缓冲放在一起，重点讲清楚&lt;strong&gt;写屏障&lt;/strong&gt;和&lt;strong&gt;读屏障&lt;/strong&gt;分别解决什么问题，以及为什么经典的&amp;quot;生产者-消费者&amp;quot;场景必须两者配合。&lt;/p&gt;
&lt;h2 id="store-buffer-回顾为什么写操作延迟可见"&gt;Store Buffer 回顾：为什么写操作&amp;quot;延迟可见&amp;quot;？ &lt;a href="#store-buffer-%e5%9b%9e%e9%a1%be%e4%b8%ba%e4%bb%80%e4%b9%88%e5%86%99%e6%93%8d%e4%bd%9c%e5%bb%b6%e8%bf%9f%e5%8f%af%e8%a7%81" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;先快速回顾 Store Buffer。当一个 CPU 核心执行 &lt;code&gt;x = 1&lt;/code&gt; 时，如果 &lt;code&gt;x&lt;/code&gt; 对应的缓存行还不在本核心的 Exclusive/Modified 状态，CPU 不会阻塞等待——它把写操作丢进 Store Buffer，然后继续执行后续指令。Store Buffer 在后台异步申请缓存行权限，拿到后再把数据刷入 Cache。&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;CPU Core 0 CPU Core 1
┌─────────────┐ ┌─────────────┐
│ store x=1 │ │ │
│ ↓ │ │ │
│ ┌─────────┐ │ ──invalidate──→ │ ┌─────────┐ │
│ │Store Buf│ │ │ │ Cache │ │
│ │ x = 1 │ │ │ │ x = 0 │ │
│ └─────────┘ │ │ └─────────┘ │
└─────────────┘ └─────────────┘
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;strong&gt;核心问题&lt;/strong&gt;：本核心能通过 store forwarding 看到自己 Store Buffer 中的新值，但&lt;strong&gt;其他核心看不到&lt;/strong&gt;，直到数据被刷入 Cache 并进入缓存一致性协议的管辖范围。&lt;/p&gt;</description></item><item><title>Store Buffer、内存屏障与缓存一致性</title><link>/blog/2026-07-16-store-buffer-and-memory-barrier/</link><pubDate>Thu, 16 Jul 2026 18:41:37 +0800</pubDate><guid>/blog/2026-07-16-store-buffer-and-memory-barrier/</guid><description>&lt;p&gt;写一个变量，其他线程（或 CPU 核心）什么时候能看到？这个看似简单的问题，答案跨越了编译器优化、CPU 微架构、缓存一致性协议等多个层次。这篇文章尝试把这条链路串起来——从你写下一行 &lt;code&gt;*p = 1&lt;/code&gt; 开始，到另一个核读到新值为止，中间到底发生了什么。&lt;/p&gt;
&lt;h2 id="c-volatile只管编译器不管硬件"&gt;C volatile：只管编译器，不管硬件 &lt;a href="#c-volatile%e5%8f%aa%e7%ae%a1%e7%bc%96%e8%af%91%e5%99%a8%e4%b8%8d%e7%ae%a1%e7%a1%ac%e4%bb%b6" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;先从一个最常见的误区开始——C/C++ 中的 &lt;code&gt;volatile&lt;/code&gt; 能保证线程可见性吗？&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;不能。&lt;/strong&gt; &lt;code&gt;volatile&lt;/code&gt; 只做一件事：告诉编译器，不要把这个变量优化到寄存器里，每次读写都要生成真正的 load/store 指令。但它对 CPU 的 cache hierarchy 和 store buffer 完全透明。&lt;/p&gt;
&lt;p&gt;一条 &lt;code&gt;*p = 1&lt;/code&gt; 对应的 &lt;code&gt;MOV&lt;/code&gt; 指令执行后，数据去了哪里？不是直接到内存，而是：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;CPU 流水线 → Store Buffer → L1 Cache → (通过缓存一致性协议扩散到其他核)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;C &lt;code&gt;volatile&lt;/code&gt; 只保证了第一步——编译器确实生成了 &lt;code&gt;MOV&lt;/code&gt; 指令。但这条指令执行后，值只是被写入了 store buffer。Store buffer 什么时候 drain 到 cache？不确定。在 drain 之前，这份数据没有被缓存一致性协议跟踪，其他核根本不知道这个写操作的存在。&lt;/p&gt;
&lt;p&gt;这也是为什么 Linux 内核文档明确说 &amp;ldquo;volatile considered harmful&amp;rdquo;——内核中用 &lt;code&gt;volatile&lt;/code&gt; 做同步几乎永远是错的，必须搭配显式的 &lt;code&gt;smp_mb()&lt;/code&gt; / &lt;code&gt;smp_wmb()&lt;/code&gt; 才有意义。&lt;code&gt;volatile&lt;/code&gt; 在内核中的真正用途是 MMIO（设备寄存器访问），而不是线程同步。&lt;/p&gt;</description></item><item><title>对volatile的认识</title><link>/blog/2022-06-26-volatile/</link><pubDate>Sun, 26 Jun 2022 23:57:00 +0800</pubDate><guid>/blog/2022-06-26-volatile/</guid><description>&lt;p&gt;关于这个我有一篇非常不错的总结，估计是全网最好的总结：&lt;a href="https://www.hitzhangjie.pro/blog/%E4%BD%A0%E4%B8%8D%E8%AE%A4%E8%AF%86%E7%9A%84cc-volatile/"&gt;你不认识的c/c++ volatile&lt;/a&gt;，虽然标题有点“博眼球”，但是内容绝对是很多高T都可能不知道的。&lt;/p&gt;
&lt;p&gt;今天翻之前整理的Linux内核文档笔记时，又看到了当时volatile相关的笔记，也是因为这个事情几年前听中心的高T分享时觉得他搞错了，才写的这篇总结。&lt;/p&gt;
&lt;p&gt;这里也放个简单的文档，系统性的强调下，认识正确了对于并发编程还是很重要的。&lt;/p&gt;
&lt;p&gt;see also &lt;a href="https://sourcegraph.com/github.com/torvalds/linux/-/blob/Documentation/process/volatile-considered-harmful.rst"&gt;linux volatile considered harmful&lt;/a&gt;，linus torvalds大佬亲笔。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;简单总结下的话就是：&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;volatile，需要volatile，尤其是对于涉及到外设访问的情况，有些外设的设备端口是通过统一编址来的，使用某些访存指令而非专用的in/out指令的话，有可能读的数据会做优化，比如放到寄存器中，硬件cpu还可能放到cache中。对于这些设备的读操作，需要避免优化才能正常工作，所以需要volatile。这在c/c++设备驱动中应该比较有用。&lt;/li&gt;
&lt;li&gt;volatile，在c/c++语言设计层面，没有保证线程可见性的任何保证，切记！它只是告知编译器不要做软件级别的寄存器优化而已，对于硬件级别的cache缓存没有任何控制。&lt;/li&gt;
&lt;li&gt;volatile，不能保证线程可见性，但是在不同的处理器平台上却是会有不同的表现，比如在x86平台上，加了volatile修饰的变量就能够保证线程可见性。为什么呢？首先加了volatile修饰后避免了寄存器优化，现在还有cache的顾虑对吧，但是x86平台比较特殊，它使用了一种称作 tso的memory model，x86多核能够看到其他核对某个cacheline的修改，因此能感知到最新写的数据，能做到线程可见性。&lt;/li&gt;
&lt;li&gt;volatile在其他平台内存模型不同，不一定能和x86一样实现线程可见性。&lt;/li&gt;
&lt;li&gt;要想实现线程可见性，编译器一般是结合语言本身的特性，为止生成一些内存屏障指令，这些屏障指令最终会触发cache的MESIF协议来使得当前核上的修改对其他核可见。&lt;/li&gt;
&lt;/ul&gt;</description></item><item><title>你不认识的cc++ volatile</title><link>/blog/2019-01-07-%E4%BD%A0%E4%B8%8D%E8%AE%A4%E8%AF%86%E7%9A%84cc-volatile/</link><pubDate>Fri, 24 Jul 2020 19:46:11 +0800</pubDate><guid>/blog/2019-01-07-%E4%BD%A0%E4%B8%8D%E8%AE%A4%E8%AF%86%E7%9A%84cc-volatile/</guid><description>&lt;style&gt;
img {
width: 680px;
}
&lt;/style&gt;
&lt;h2 id="1-令人困惑的volatile"&gt;1. 令人困惑的volatile &lt;a href="#1-%e4%bb%a4%e4%ba%ba%e5%9b%b0%e6%83%91%e7%9a%84volatile" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;volatile字面意思是“&lt;strong&gt;不稳定的、易变的&lt;/strong&gt;”，不少编程语言中存在volatile关键字，也有共同之处，如“&lt;strong&gt;表示程序执行期间数据可能会被外部操作修改&lt;/strong&gt;”，如被外设修改或者被其他线程修改等。这只是字面上给我们的一般性认识，然而具体到不同的编程语言中volatile的语义可能相差甚远。&lt;/p&gt;
&lt;p&gt;很多人以为自己精通CC++，但是被问起volatile的时候却无法清晰、果断地表明态度，那只能说明还是处在“从入门到精通”的路上，如果了解一门语言常见特性的使用、能够写健壮高效的程序就算精通的话，那实在是太藐视“大师”的存在了。从一个volatile关键字折射出了对CC++标准、编译器、操作系统、处理器、MMU各个方面的掌握程度。&lt;/p&gt;
&lt;p&gt;几十年的发展，很多开发者因为自己的偏见、误解，或者对某些语言特性（如Java中的volatile语义）的根深蒂固的认识，赋予了CC++ volatile本不属于它的能力，自己却浑然不知自己犯了多大的一个错误。&lt;/p&gt;
&lt;p&gt;我曾经以为CC++中volatile可以保证线程可见性，因为Java中是这样的，直到后来阅读Linux内核看到Linus Torvards的一篇文档，他强调了volatile可能带来的坏处“任何使用volatile的地方，都可能潜藏了一个bug”，我为他的“危言耸听”感到吃惊，所以我当时搜索了不少资料来求证CC++ volatile的能力，事后我认为CC++ volatile不能保证线程可见性。但是后来部门内一次分享，分享中提到了volatile来保证线程可见性，我当时心存疑虑，事后验证时犯了一个错误导致我错误地认为volatile可以保证线程可见性。直到我最近翻阅以前的笔记，翻到了几年前对volatile的疑虑……我决定深入研究下这个问题，以便能顺利入眠。&lt;/p&gt;
&lt;h2 id="2-从规范认识volatile"&gt;2. 从规范认识volatile &lt;a href="#2-%e4%bb%8e%e8%a7%84%e8%8c%83%e8%ae%a4%e8%af%86volatile" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;以常见的编程语言C、C++、Java为例，它们都有一个关键字volatile，但是对volatile的定义却并非完全相同。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;Java中对volatile的定义：&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;8.3.1.4. &lt;code&gt;volatile&lt;/code&gt; Fields&lt;/p&gt;
&lt;p&gt;The Java programming language allows threads to access shared variables (&lt;a href="https://docs.oracle.com/javase/specs/jls/se7/html/jls-17.html#jls-17.1"&gt;§17.1&lt;/a&gt;). As a rule, to ensure that shared variables are consistently and reliably updated, a thread should ensure that it has exclusive use of such variables by obtaining a lock that, conventionally, enforces mutual exclusion for those shared variables.&lt;/p&gt;</description></item><item><title>Cache 一致性协议</title><link>/blog/2019-01-04-kernel-cache%E4%B8%80%E8%87%B4%E6%80%A7%E5%8D%8F%E8%AE%AE/</link><pubDate>Fri, 04 Jan 2019 12:18:24 +0800</pubDate><guid>/blog/2019-01-04-kernel-cache%E4%B8%80%E8%87%B4%E6%80%A7%E5%8D%8F%E8%AE%AE/</guid><description>&lt;p&gt;cache一致性实现机制，主要包括基于snooping和基于directory两种机制，一致性机制解决的是如何观测cpu read/write操作事件，两种各有优缺点：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;snooping，在cpu数量、core数量比较少的情况下，snooping可以减少req/rsp的数量，是基于广播的，效率比较高，但是在cpu、core数量比较多的情况下难以水平扩展，为了满足广播需要增加依赖的总线带宽；&lt;/li&gt;
&lt;li&gt;directory，在cpu数量、core数量比较多的情况下，更加适用，可以平行扩展cpu、core数量，不需要增加总线带宽，但是由于是计算cpu或者core之间的点对点请求，没有广播，req/rsp次数会比较多，会增加一定的延时；&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;cache一致性算法，主要包括write-through（写直达）或者write-back（写回），write-back并不是当cache中数据更新时立即写回，而是在稍后的某个时机再写回。写直达会严重降低cpu吞吐量，所以现如今的主流处理器中通常采用write-back算法。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;write-through&lt;/li&gt;
&lt;li&gt;write-back
&lt;ul&gt;
&lt;li&gt;write-invalidate，当某个core（如core 1）的cache被修改为最新数据后，总线观测到更新，将写事件同步到其他core（如core n），将其他core对应相同内存地址的cache entry标记为invalidate，后续core n继续读取相同内存地址数据时，发现已经invalidate，会再次请求内存中最新数据。&lt;/li&gt;
&lt;li&gt;write-update，当某个core（如core 1）的cache被修改为最新数据后，将写事件同步到其他core，此时其他core（如core n）立即读取最新数据（如更新为core 1中数据）。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="coherence-mechanismsedit"&gt;Coherence mechanisms[&lt;a href="https://en.wikipedia.org/w/index.php?title=Cache_coherence&amp;amp;action=edit&amp;amp;section=3"&gt;edit&lt;/a&gt;] &lt;a href="#coherence-mechanismsedit" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;The two most common mechanisms of ensuring coherency are &lt;em&gt;snooping&lt;/em&gt; and &lt;em&gt;directory-based&lt;/em&gt;, each having their own benefits and drawbacks. Snooping based protocols tend to be faster, if enough &lt;a href="https://en.wikipedia.org/wiki/Memory_bandwidth"&gt;bandwidth&lt;/a&gt; is available, since all transactions are a request/response seen by all processors. The drawback is that snooping isn&amp;rsquo;t scalable. Every request must be broadcast to all nodes in a system, meaning that as the system gets larger, the size of the (logical or physical) bus and the bandwidth it provides must grow. Directories, on the other hand, tend to have longer latencies (with a 3 hop request/forward/respond) but use much less bandwidth since messages are point to point and not broadcast. For this reason, many of the larger systems (&amp;gt;64 processors) use this type of cache coherence.&lt;/p&gt;</description></item></channel></rss>