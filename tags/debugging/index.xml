<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Debugging on</title><link>/tags/debugging/</link><description>Recent content in Debugging on</description><generator>Hugo -- gohugo.io</generator><language>en-US</language><lastBuildDate>Fri, 23 Jan 2026 00:00:00 +0800</lastBuildDate><atom:link href="/tags/debugging/index.xml" rel="self" type="application/rss+xml"/><item><title>如何让 Go 工具链生成 DWARF 调试信息</title><link>/blog/2026-01-23-go-toolchain-dwarf/</link><pubDate>Fri, 23 Jan 2026 00:00:00 +0800</pubDate><guid>/blog/2026-01-23-go-toolchain-dwarf/</guid><description>&lt;p&gt;如果你想调试 Go 编译工具链本身，会发现默认的 Go 正式版在构建 &lt;code&gt;go/src/cmd/...&lt;/code&gt; 时不会生成 DWARF 调试信息。如果想让 Go toolchain 也生成 DWARF 调试信息，方便做相关研究时通过调试器跟踪，该怎么办？&lt;/p&gt;
&lt;hr&gt;
&lt;h2 id="版本检查和-isrelease-设置"&gt;版本检查和 isRelease 设置 &lt;a href="#%e7%89%88%e6%9c%ac%e6%a3%80%e6%9f%a5%e5%92%8c-isrelease-%e8%ae%be%e7%bd%ae" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;在 &lt;code&gt;src/cmd/dist/build.go&lt;/code&gt; 第 272-273 行：&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-go"&gt;goversion := findgoversion()
isRelease = strings.HasPrefix(goversion, &amp;quot;release.&amp;quot;) || strings.HasPrefix(goversion, &amp;quot;go&amp;quot;)
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id="dwarf-调试信息禁用逻辑"&gt;DWARF 调试信息禁用逻辑 &lt;a href="#dwarf-%e8%b0%83%e8%af%95%e4%bf%a1%e6%81%af%e7%a6%81%e7%94%a8%e9%80%bb%e8%be%91" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;同一文件第 1346-1352 行：&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-go"&gt;if isRelease || os.Getenv(&amp;quot;GO_BUILDER_NAME&amp;quot;) != &amp;quot;&amp;quot; {
// Add -trimpath for reproducible builds of releases.
// Include builders so that -trimpath is well-tested ahead of releases.
// Do not include local development, so that people working in the
// main branch for day-to-day work on the Go toolchain itself can
// still have full paths for stack traces for compiler crashes and the like.
env = append(env, &amp;quot;GOFLAGS=-trimpath -ldflags=-w -gcflags=cmd/...=-dwarf=false&amp;quot;)
}
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id="工作原理"&gt;工作原理 &lt;a href="#%e5%b7%a5%e4%bd%9c%e5%8e%9f%e7%90%86" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;ol&gt;
&lt;li&gt;&lt;code&gt;findgoversion()&lt;/code&gt; 读取 &lt;code&gt;VERSION&lt;/code&gt; 文件的第一行作为版本字符串。&lt;/li&gt;
&lt;li&gt;如果版本字符串以 &lt;code&gt;&amp;quot;release.&amp;quot;&lt;/code&gt; 或 &lt;code&gt;&amp;quot;go&amp;quot;&lt;/code&gt; 开头，&lt;code&gt;isRelease&lt;/code&gt; 被设为 &lt;code&gt;true&lt;/code&gt;。&lt;/li&gt;
&lt;li&gt;当 &lt;code&gt;isRelease&lt;/code&gt; 为 &lt;code&gt;true&lt;/code&gt; 时，会添加 &lt;code&gt;-dwarf=false&lt;/code&gt;，从而禁用 DWARF 调试信息。&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id="绕过方法"&gt;绕过方法 &lt;a href="#%e7%bb%95%e8%bf%87%e6%96%b9%e6%b3%95" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;在 &lt;code&gt;VERSION&lt;/code&gt; 文件的第一行前加前缀（如 &lt;code&gt;&amp;quot;notes/&amp;quot;&lt;/code&gt; 或 &lt;code&gt;&amp;quot;tests/&amp;quot;&lt;/code&gt;），例如：&lt;/p&gt;</description></item><item><title>bisect：二分定位多特性开关中引入的bug</title><link>/blog/2025-03-14-debug-bisect/</link><pubDate>Fri, 14 Mar 2025 00:00:00 +0800</pubDate><guid>/blog/2025-03-14-debug-bisect/</guid><description>&lt;p&gt;开发过程中经常遇到这种情况：项目里有多个特性开关，某次发布后测试报了个 bug，但你不知道是哪个开关、或者哪几个开关的组合导致的。如果只有两三个开关，手动 enable/disable 一个个试也行。但如果开关数量多、迭代频率高、开关之间还相互影响，手动排查的效率就完全跟不上了。&lt;/p&gt;
&lt;p&gt;这个问题 Go 编译器团队也遇到过——编译器的每个优化 pass 都可能引入 bug，而且这些优化之间会相互触发。他们搞了一套叫 bisect 的定位方法，Russ Cox 专门写了篇文章介绍&lt;sup id="fnref:1"&gt;&lt;a href="#fn:1" class="footnote-ref" role="doc-noteref"&gt;1&lt;/a&gt;&lt;/sup&gt;，我在 &lt;a href="https://github.com/hitzhangjie/golang-debugger-book"&gt;golang-debugger-book&lt;/a&gt; 里也做过系统性的拆解。后来这套方法被沉淀成了 Go 官方的 bisect 工具和库&lt;sup id="fnref:2"&gt;&lt;a href="#fn:2" class="footnote-ref" role="doc-noteref"&gt;2&lt;/a&gt;&lt;/sup&gt;，任何 Go 项目都能直接用。&lt;/p&gt;
&lt;p&gt;本文通过一个实际的例子演示怎么把 bisect 接入项目，以及怎么用它快速定位问题开关。&lt;/p&gt;
&lt;h1 id="场景udp-echo-server-改造为支持-tcp"&gt;场景：UDP echo server 改造为支持 TCP &lt;a href="#%e5%9c%ba%e6%99%afudp-echo-server-%e6%94%b9%e9%80%a0%e4%b8%ba%e6%94%af%e6%8c%81-tcp" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;p&gt;假设我们维护着一个 UDP echo server，现在要做一次改造，让它也支持 TCP。改动的点比较多：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;新增 TCP listener 和 accept 循环&lt;/li&gt;
&lt;li&gt;连接处理中引入 buffered write&lt;/li&gt;
&lt;li&gt;新增字符串反转功能（产品要求）&lt;/li&gt;
&lt;li&gt;还顺手调整了连接关闭逻辑&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;为了控制上线风险，每个改动点都有一个开关。代码大概长这样：&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-go"&gt;var (
useTCP = true // 开关 1：是否启用 TCP
useBufferedWrite = true // 开关 2：是否使用 buffered write
reverseEcho = true // 开关 3：是否反转回显内容
skipClose = false // 开关 4：是否跳过连接关闭
)
func main() {
if useTCP {
go startTCPListener()
}
go startUDPListener()
select {}
}
func handleConnection(conn net.Conn) {
defer func() {
if !skipClose {
conn.Close()
}
}()
buf := make([]byte, 1024)
for {
n, err := conn.Read(buf)
if err != nil {
return
}
response := processResponse(string(buf[:n]))
if useBufferedWrite {
writer := bufio.NewWriter(conn)
writer.WriteString(response)
// BUG: 忘了 writer.Flush()
} else {
conn.Write([]byte(response))
}
}
}
func processResponse(data string) string {
if reverseEcho {
words := strings.Split(data, &amp;quot; &amp;quot;)
for i := range words {
words[i] = reverseString(words[i])
}
return strings.Join(words, &amp;quot; &amp;quot;)
}
return data
}
func reverseString(s string) string {
runes := []rune(s)
// BUG: 循环条件应该是 len(runes)/2
for i := 0; i &amp;lt; len(runes)/2-1; i++ {
runes[i], runes[len(runes)-1-i] = runes[len(runes)-1-i], runes[i]
}
return string(runes)
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;改造完成后一跑测试：client 发过去的数据，server 没回。这里有两个 bug——buffered write 忘了 flush，字符串反转的循环条件也写错了。&lt;/p&gt;</description></item><item><title>bisect：二分定位背后的协议与算法</title><link>/blog/2025-03-14-debug-bisect-internals/</link><pubDate>Fri, 14 Mar 2025 00:00:00 +0800</pubDate><guid>/blog/2025-03-14-debug-bisect-internals/</guid><description>&lt;p&gt;&lt;a href="/blog/debug/2025-03-14-debug-bisect/"&gt;上一篇文章&lt;/a&gt; 演示了怎么把 bisect 接进项目来定位问题开关，走的是实操路线。这篇拆一下 bisect 的实现原理——它怎么跟 target 程序通信、pattern 怎么编码开关状态、hash 位数怎么自适应、以及二分搜索的过程。&lt;/p&gt;
&lt;p&gt;理解了这些，下次遇到类似的&amp;quot;多因素故障定位&amp;quot;场景，你就能自己设计一套类似的东西了。&lt;/p&gt;
&lt;h1 id="协议设计"&gt;协议设计 &lt;a href="#%e5%8d%8f%e8%ae%ae%e8%ae%be%e8%ae%a1" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;p&gt;bisect 的设计核心是一个简单的协议：&lt;strong&gt;工具（bisect）通过环境变量下发指令，target 程序通过 stderr 上报状态，最终通过退出码告知结果&lt;/strong&gt;。&lt;/p&gt;
&lt;p&gt;不依赖任何外部设施——没有 IPC、没有 socket、没有文件。这让它几乎可以嵌入任何构建和测试流程。&lt;/p&gt;
&lt;h2 id="markertarget-如何报告点位"&gt;Marker：target 如何报告点位 &lt;a href="#markertarget-%e5%a6%82%e4%bd%95%e6%8a%a5%e5%91%8a%e7%82%b9%e4%bd%8d" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;当 target 程序在某个开关点决定启用还是禁用时，它必须向 stderr 打印一个 marker。bisect 库提供了 &lt;code&gt;AppendMarker&lt;/code&gt; 来生成这行输出：&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-go"&gt;func AppendMarker(dst []byte, id uint64) []byte {
return fmt.Appendf(dst, &amp;quot;[bisect-match 0x%016x]&amp;quot;, id)
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;实际输出格式：&lt;/p&gt;
&lt;pre&gt;&lt;code&gt;[bisect-match 0x0000000000000001] feature description
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;hash ID 固定以 16 位 hex 输出（完整的 64 位），保证 marker 在不同 pattern 下都携带完整的点位身份。&lt;/p&gt;
&lt;p&gt;bisect 工具端通过 &lt;code&gt;CutMarker&lt;/code&gt; 解析这些行——扫描每行中的 &lt;code&gt;[bisect-match ...]&lt;/code&gt;，提取 hash ID 并将标记从文本中移除：&lt;/p&gt;</description></item><item><title>实现k8s应用远程调试</title><link>/blog/2022-04-08-%E5%AE%9E%E7%8E%B0k8s%E5%BA%94%E7%94%A8%E8%BF%9C%E7%A8%8B%E8%B0%83%E8%AF%95/</link><pubDate>Fri, 08 Apr 2022 15:45:20 +0800</pubDate><guid>/blog/2022-04-08-%E5%AE%9E%E7%8E%B0k8s%E5%BA%94%E7%94%A8%E8%BF%9C%E7%A8%8B%E8%B0%83%E8%AF%95/</guid><description>&lt;p&gt;最近在支持团队测试左移、测试规范、CI/CD流水线方面的一些工作，针对开发人员发现问题、定位问题、修复问题这个过程产生了一点新的想法，尤其是在整体上云后的大背景下，我认为值得和大家一起探讨下，如何借助远程调试更快速地定位k8s应用问题。&lt;/p&gt;
&lt;h1 id="1-问题背景"&gt;1 问题背景 &lt;a href="#1-%e9%97%ae%e9%a2%98%e8%83%8c%e6%99%af" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;p&gt;现在公司大部分业务都上云了，服务部署基本上也k8s容器化了，解决了一些老大难的问题，本文不讨论k8s的好处，我们讨论下开发k8s应用（比如微服务）时开发侧可能碰到的一些问题。前段时间我开发了一个统计模调接口成功率的服务，部署在123平台，因为服务代码有些隐晦的bug排查修复花了些时间，这个过程中就开始思考为什么我对go、trpc很熟的情况下定位问题还这么麻烦呢？于是就有了接下来的探索以及这篇总结。&lt;/p&gt;
&lt;h1 id="2-经常遇到的问题"&gt;2 经常遇到的问题 &lt;a href="#2-%e7%bb%8f%e5%b8%b8%e9%81%87%e5%88%b0%e7%9a%84%e9%97%ae%e9%a2%98" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;p&gt;如果一件事情，短时间内需要人肉重复很多遍，就很容易让人抓狂，比如这次开发体验很快就让我抓狂了：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;测试服务发现服务表现不正常，准备定位bug&lt;/li&gt;
&lt;li&gt;查看错误日志初步锁定问题范围，重新走读代码进一步缩小问题范围&lt;/li&gt;
&lt;li&gt;修改代码、提交代码，构建镜像、发布，然后重新测试&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;排查问题不只是这么轻描淡写，简单3步就搞定了：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;可能是bug不容易定位，在外部数据满足某种条件下或者遇到某种事件时才会触发bug；&lt;/li&gt;
&lt;li&gt;可能是测试用例覆盖不够，通过上述方法定位并解决了问题1，但是后面发现了问题2；&lt;/li&gt;
&lt;li&gt;可能是日志信息不够，错误异常分支没有日志，或者日志信息不全；&lt;/li&gt;
&lt;li&gt;微服务架构事务处理会跨越多个服务，可能要结合tracing、logging、metrics多个系统联合排查；&lt;/li&gt;
&lt;li&gt;修改代码后提交并构建发布，可能涉及到CI/CD环节，流水线耗时较长；&lt;/li&gt;
&lt;li&gt;123平台提供了dtools来快速构建、patch，但是破坏了测试环境稳定性（缺少镜像）&lt;/li&gt;
&lt;li&gt;TKE有同学基于七彩石配置下发实现的替换工具，存在dtools类似的问题；&lt;/li&gt;
&lt;li&gt;……&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;总之，这个排查过程可能要涉及到多轮人肉操作，作为一个VIM党连上下移动都觉得用滑动鼠标、按上下左右是种低效的操作，更不用说让我在各个系统中间（而且系统衔接当前也还有较大空间）切来切去了。做了这么多“额外”的操作，才能渐渐去逼近真相、解决真正的问题。&lt;/p&gt;
&lt;h1 id="3-降低问题复杂度"&gt;3 降低问题复杂度 &lt;a href="#3-%e9%99%8d%e4%bd%8e%e9%97%ae%e9%a2%98%e5%a4%8d%e6%9d%82%e5%ba%a6" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;p&gt;这里先声明下，并不是说通过日志这种方式排查问题不好，主要还是要看待解决问题的复杂度，如果加几行日志就可以轻松缩小问题域并解决，那自然是好的。但是实际情况是，并不是所有问题都这么容易解决，而且也确实需要多考虑一些其他影响，比如dtools对测试环境的破坏（覆盖了镜像、忘了打镜像怎么办），频繁测试严格走CI/CD的耗时，等等。&lt;/p&gt;
&lt;p&gt;包括现在提倡的测试左移，单测覆盖、接口测试、集成测试等等，测试是门艺术，远比我之前肤浅的认识要重要，除了质量也要关注效率，做这么多其实也是像在投入时间、迭代效率、软件质量、团队构成等因素之间寻求一种平衡，降低整体复杂度。现在有些团队已经不再将单测覆盖率当做硬指标了，而是通过结合多种测试手段来寻求这种平衡。&lt;/p&gt;
&lt;p&gt;很多人对TDD有认识，但是在实际开发过程中并不会真的去做，代码语句覆盖、分支覆盖情况可能并不高，而且对某些corner cases可能靠正常思路也很难构造出来，可能要结合fuzz testing来协助。&lt;/p&gt;
&lt;p&gt;说这么多，只是为了说明一点，发现问题、走查日志、走查代码、修复提交、构建发布、重新测试，可能是我们每个开发同学解决定位解决bug时高频出现的操作流程，没有好的问题定位工具支持，对宝贵的开发资源就是种浪费。&lt;/p&gt;
&lt;p&gt;我看了下外部的很多团队在k8s开发方面的一些实践，这个过程感觉是可以优化的，那就是让开发k8s应用的我们获得本地调试能力，让远程的一些“不确定性的因素”变成本地“肉眼可见”的观测，问题解决起来就方便多了。尤其是对某些testcase，可能需要反复往前、往后翻代码才能定位到问题，这种在将mozilla rr（record and replay）作为调试器backend的加成下优势会非常明显。&lt;/p&gt;
&lt;h1 id="4-远程调试k8s应用"&gt;4 远程调试k8s应用 &lt;a href="#4-%e8%bf%9c%e7%a8%8b%e8%b0%83%e8%af%95k8s%e5%ba%94%e7%94%a8" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;p&gt;前面我们说了开发定位bug时大致的手段，并且强调了在问题比较复杂时，我们可能会通过多次“修改代码、构建发布、测试”这样的流程来逼近bug，这种场景下，如果能用调试器来跟踪下应用的执行过程，观测下执行流程、变量信息等是否符合预期，缩小问题域的过程会快很多，扩展阅读部分给出了一些业界团队的实践，供参考。&lt;/p&gt;
&lt;h2 id="调试器基础"&gt;调试器基础 &lt;a href="#%e8%b0%83%e8%af%95%e5%99%a8%e5%9f%ba%e7%a1%80" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;对于一般的应用程序调试器大致可以分为两种类型：指令级调试器、符号级调试器，大家平时调试用的gdb、delve等就是符号级调试器，当然他们也具备一定的指令级调试能力。现代符号级调试器架构一般分为frontend、backend，二者通过service层进行通信（如借助rpc或者pipe），frontend主要是完成与用户的交互、展示，backend完成对tracee（被调试线程）的实际控制。&lt;/p&gt;
&lt;p&gt;以go语言的符号级调试器go-delve/delve为例，其分为frontend、backend，frontend可以是命令行、gdlv图形界面、vscode、goland等，backend针对不同的平台有多种实现，如借助平台的delve native实现、借助其他调试器能力gdbserver、mozilla rr等。本地调试frontend和backend之间通信通过net.pipe，远程通信通过json-rpc，如果是考虑到与vscode、goland集成的话则需要考虑类似DAP（debugger adapter protocol）的方式。&lt;/p&gt;
&lt;p&gt;ps: 这里不过多展开了，对调试器设计实现感兴趣的朋友，可以参考我的电子书（最后一章待完成）：https://www.hitzhangjie.pro/debugger101.io/&lt;/p&gt;
&lt;p&gt;大家用的vscode插件、go-delve/delve调试器，其实就是先起个dlv backend以server的形式监听，协议就是DAP，通过vscode调试的时候，会通过DAP协议与dlv backend交互，dlv backend对被调试进程进行控制（如控制单步执行、读写内存等），大致就是这样的，对于远程调试k8s应用，也是这样的过程。&lt;/p&gt;
&lt;h2 id="远程调试演示"&gt;远程调试演示 &lt;a href="#%e8%bf%9c%e7%a8%8b%e8%b0%83%e8%af%95%e6%bc%94%e7%a4%ba" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;首先给大家简单实操演示下远程调试k8s应用，最后大家会意识到这个是完全可以实现的，而且可以做的更方便易用。&lt;/p&gt;
&lt;h3 id="示例工程说明"&gt;示例工程说明 &lt;a href="#%e7%a4%ba%e4%be%8b%e5%b7%a5%e7%a8%8b%e8%af%b4%e6%98%8e" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;下面以一个简单的工程作为示例，实操下如何远程调试k8s应用，这个工程只有这么几个文件：&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-bash"&gt;/Volumes/kubernetes/debugging-go-app-in-k8s $ tree .
.
├── Dockerfile
├── Makefile
├── app
└── main.go
0 directories, 4 files
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;这里的main.go，是一个http服务，模拟我们的微服务，可以长期运行并接受用户请求。&lt;/p&gt;</description></item></channel></rss>