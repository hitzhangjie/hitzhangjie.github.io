<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Half-Closed on</title><link>/tags/half-closed/</link><description>Recent content in Half-Closed on</description><generator>Hugo -- gohugo.io</generator><language>en-US</language><lastBuildDate>Thu, 20 Dec 2018 23:11:29 +0800</lastBuildDate><atom:link href="/tags/half-closed/index.xml" rel="self" type="application/rss+xml"/><item><title>网络编程：tcpconn.Write 未报错，实际发送失败</title><link>/blog/2018-12-20-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B_write-closed-tcpconn/</link><pubDate>Thu, 20 Dec 2018 23:11:29 +0800</pubDate><guid>/blog/2018-12-20-%E7%BD%91%E7%BB%9C%E7%BC%96%E7%A8%8B_write-closed-tcpconn/</guid><description>&lt;h1 id="问题背景"&gt;问题背景 &lt;a href="#%e9%97%ae%e9%a2%98%e8%83%8c%e6%99%af" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;blockquote&gt;
&lt;p&gt;注意half-closed强调的是tcp全双工通信方向上关闭了其中一个方向的通信，如A、B通信，A调用shutdown(fd, write)不再给B发数据，但是仍然可以从B收数据。有时候，也会听到一些误用的情况，如用来描述一个tcpconn在某个peer侧单方面关闭了这个连接，比如A、B通信，A调用了close(fd)，这不叫half-close connection，这叫close connection。
有时候，我们必须咬文嚼字，因为只有严格区分每一种case，我们才能真正对齐我们在讨论哪一种技术细节问题。模糊、乱用专业术语，只会让混乱更加混乱，鸡同鸭讲完全不在一个频道上。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;p&gt;在本文这个要讨论的问题背景下，框架要检测“对端写半连接关闭”以及“对端连接关闭”的情景，从检测方法上而言，方法是统一的，那就是直接通过系统调用进行非阻塞读，然后基于结果进行判断。如果检测到EOF，那连接至少肯定是对端写关闭了，也可能是全关闭了。对于网络框架这种通过同一个连接发送请求、接收响应的情景而言，这样的连接不健康（不能收取响应），需要从连接池剔除掉。&lt;/p&gt;
&lt;p&gt;在过去写这篇文章的时候，我也对half-close tcpconn这个术语随意使用，有时候强调某个方向关闭，有时候表示socket层面关闭。严格来说是不准确的，这里强调下。在我们的框架中不会调用shutdown，只会调用close。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;tcp client: write to a closed tcp connection!&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;这里探讨一下这个问题，Write to a closed tcp connection的问题。在深入讨论这些问题之前，首先要了解tcp state diagram，为此文末特地附上了经典的tcp状态转换图。&lt;/p&gt;
&lt;p&gt;我们的场景是这样的，tcp server已经启动，然后tcp client主动建立连接请求，连接成功建立后，tcp client并不立即发送数据而是等待一段时间之后才会发送数据（这种在client端的tcp连接池中非常常见），tcp server端为了防止连接被滥用，会每隔30s钟检查一下tcp连接是否空闲，如果两次检查都发现tcp连接空闲则主动将连接关闭。&lt;/p&gt;
&lt;h1 id="原因分析"&gt;原因分析 &lt;a href="#%e5%8e%9f%e5%9b%a0%e5%88%86%e6%9e%90" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;p&gt;此时tcp server端会调用 &lt;code&gt;conn.Close()&lt;/code&gt;方法，该方法最终会导致传输层发送tcp FIN包给对端，tcp client这边的机器收到FIN包后会回一个ACK，然后呢？tcp client不会继续发FIN包给tcp server吗？不会！仅此而已。问题就是这么诞生的，什么问题呢，tcp client仍然可以发包，但是 &lt;code&gt;n, err := tcpconn.Write(...)&lt;/code&gt;这个时候并不会检测到 &lt;code&gt;err != nil&lt;/code&gt;，只有等到 &lt;code&gt;n, err := tcpconn.Read(...)&lt;/code&gt;的时候才会发现err为io.EOF，这个时候才能判断得知tcp server已经把连接销毁了。&lt;/p&gt;
&lt;p&gt;从RPC框架角度而言，希望为client维护的tcp连接池是高效可用的，所以想对上述情况下的客户端连接进行检测，避免连接池中存在上述被tcp server关闭的连接。&lt;/p&gt;
&lt;p&gt;再简单总结下tcp server、tcp client两端的不同处理逻辑：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;从tcp server的视角来看，&lt;/p&gt;
&lt;p&gt;tcp server调用的conn.Close()，对应的是系统调用close，tcp server端认为它已经彻底关闭这个连接了！&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;从tcp client的视角来看，&lt;/p&gt;
&lt;p&gt;这个连接是我主动建立的，我还没有给你发送FIN包发起关闭序列呢，因此这个连接仍然是可以使用的。tcp client认为tcp server只是关闭了写端，没有关闭读端，因此tcp client仍然是可写的，并且socket被设置成了nonblocking，conn.Write()仍然是成功返回的，返回的err == nil。但是当真正传输层执行数据发送的时候，tcp server端认为这个连接已销毁，因此会返回RST！这个时候上层go代码因为已经返回已经感知不到写的时候存在错误，这个RST会将tcp client端的socket标记为已关闭。下次tcpconn.Read的时候就能感知到io.EOF错误了，如果再发起一次tcpconn.Write也可以立即返回错误。&lt;/p&gt;</description></item></channel></rss>