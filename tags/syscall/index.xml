<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Syscall on</title><link>/tags/syscall/</link><description>Recent content in Syscall on</description><generator>Hugo -- gohugo.io</generator><language>en-US</language><lastBuildDate>Tue, 06 Jan 2026 00:00:00 +0800</lastBuildDate><atom:link href="/tags/syscall/index.xml" rel="self" type="application/rss+xml"/><item><title>Go 1.26 os.ProcessHandle：进程管理的能力增强</title><link>/blog/2026-01-06-go-process-handle/</link><pubDate>Tue, 06 Jan 2026 00:00:00 +0800</pubDate><guid>/blog/2026-01-06-go-process-handle/</guid><description>&lt;p&gt;在 Go 1.26 中引入的 &lt;code&gt;os.ProcessHandle&lt;/code&gt; 是对进程管理能力的重大增强。在此之前，Go 对外部进程的控制主要依赖于进程 ID（PID），但在高并发或长运行的系统中，PID 存在**重用（Reuse）**的风险。&lt;/p&gt;
&lt;p&gt;&lt;code&gt;ProcessHandle&lt;/code&gt; 的核心价值在于它提供了一个&lt;strong&gt;确定性的、受生命周期管理的句柄&lt;/strong&gt;，确保你操作的正是你启动的那个进程。&lt;/p&gt;
&lt;hr&gt;
&lt;h2 id="1-processhandle-解决了什么痛点"&gt;1. ProcessHandle 解决了什么痛点？ &lt;a href="#1-processhandle-%e8%a7%a3%e5%86%b3%e4%ba%86%e4%bb%80%e4%b9%88%e7%97%9b%e7%82%b9" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;在旧版本中，我们通常这样操作：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;使用 &lt;code&gt;exec.Command&lt;/code&gt; 启动进程。&lt;/li&gt;
&lt;li&gt;通过 &lt;code&gt;cmd.Process.Pid&lt;/code&gt; 获取 PID。&lt;/li&gt;
&lt;li&gt;之后如果想给进程发信号，系统会根据 PID 查找。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;&lt;strong&gt;风险点：&lt;/strong&gt; 如果进程已经意外退出，而操作系统迅速将该 PID 分配给了另一个新进程（如浏览器或数据库），你此时调用 &lt;code&gt;process.Signal()&lt;/code&gt; 可能会误杀掉无关的系统进程。&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;&lt;code&gt;ProcessHandle&lt;/code&gt; 的优势：&lt;/strong&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;句柄唯一性：&lt;/strong&gt; 它持有内核层面的句柄（如 Windows 的 HANDLE 或 Linux 的 PidFd）。只要句柄没释放，系统就不会重用该 PID。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;资源安全：&lt;/strong&gt; 通过 &lt;code&gt;WithHandle&lt;/code&gt; 机制，确保在你的逻辑执行完之前，句柄保持有效。&lt;/li&gt;
&lt;/ul&gt;
&lt;hr&gt;
&lt;h2 id="2-你可以用它做什么"&gt;2. 你可以用它做什么？ &lt;a href="#2-%e4%bd%a0%e5%8f%af%e4%bb%a5%e7%94%a8%e5%ae%83%e5%81%9a%e4%bb%80%e4%b9%88" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;h3 id="安全的多进程协作"&gt;安全的多进程协作 &lt;a href="#%e5%ae%89%e5%85%a8%e7%9a%84%e5%a4%9a%e8%bf%9b%e7%a8%8b%e5%8d%8f%e4%bd%9c" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;如果你在开发一个 Master-Worker 架构的服务，Master 需要频繁监控和操作 Worker 进程。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;以前：&lt;/strong&gt; 担心 Worker 挂了后 PID 被别的程序占了。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;现在：&lt;/strong&gt; 持有 &lt;code&gt;ProcessHandle&lt;/code&gt;，你可以百分之百确定 &lt;code&gt;Signal&lt;/code&gt; 或 &lt;code&gt;Wait&lt;/code&gt; 作用的对象是正确的。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="跨进程句柄传递withhandle"&gt;跨进程句柄传递（WithHandle） &lt;a href="#%e8%b7%a8%e8%bf%9b%e7%a8%8b%e5%8f%a5%e6%9f%84%e4%bc%a0%e9%80%92withhandle" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;&lt;code&gt;os.Process.WithHandle(f)&lt;/code&gt; 允许你将句柄的所有权逻辑封装起来。&lt;/p&gt;</description></item><item><title>syscall：how does go runtime handles syscall</title><link>/blog/2021-06-06-%E8%BF%90%E8%A1%8C%E6%97%B6_how-go-handles-syscall/</link><pubDate>Sun, 06 Jun 2021 10:17:34 +0800</pubDate><guid>/blog/2021-06-06-%E8%BF%90%E8%A1%8C%E6%97%B6_how-go-handles-syscall/</guid><description>&lt;h1 id="1-how-go-runtime-handle-syscall-"&gt;1 How go runtime handle syscall ? &lt;a href="#1-how-go-runtime-handle-syscall-" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;p&gt;最近遇到个线上服务频繁陷入系统调用导致go运行时创建了大量线程，影响到了服务质量，定位、解决问题之后，希望能进一步探究go运行时处理系统调用的过程，以便加深理解。参考了不少网友的分享，特别是知乎Golang Inernal专栏，结合个人的学习理解在此整理记录一下，与大家分享。&lt;/p&gt;
&lt;h2 id="11-前言"&gt;1.1 前言 &lt;a href="#11-%e5%89%8d%e8%a8%80" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;在开始结合源码进行分析之前，先做下简单的介绍，方便先从整体上把握go对系统调用的处理过程，然后从第二部分开始，再结合源码介绍具体的细节。&lt;/p&gt;
&lt;p&gt;系统调用分为阻塞系统调用、非阻塞系统调用，go里面对这些系统调用有归类整理，详见源文件：&lt;code&gt;/src/syscall/syscall_linux_amd64.go&lt;/code&gt;。&lt;/p&gt;
&lt;p&gt;如下图所示，sys开头的表示的是阻塞系统调用，会调用Syscall，以sysnb开头的是非阻塞系统调用，会调用RawSyscall，关于Syscall和RawSyscall的区别下面整理。阻塞型的系统调用本身会阻塞线程，为了避免线程阻塞导致协程不可调度，golang运行时要感知这样的系统调用并做特殊处理，非阻塞的系统调用直接调即可，不需要golang运行时参与。
&lt;img alt="系统调用列表节选" src="assets/syscall/1545292741_96.png" class="myimg"&gt;&lt;/p&gt;
&lt;p&gt;Syscall定义在asm_linux_amd64.s里面，代码中有&lt;code&gt;runtime.entersyscall(SB)&lt;/code&gt;和&lt;code&gt;runtime.exitsyscall(SB)&lt;/code&gt;函数调用，这个是与golang运行时进行交互的，用于通知golang运行时我即将发起或者退出一个系统调用。&lt;/p&gt;
&lt;p&gt;对于会导致阻塞的系统调用，都要通过Syscall来调用来通知golang运行时，以便golang运行时做处理，如创建新的物理线程调度器其它的goroutine，避免整个进程无线程可调度而最终被sysmon杀死进程。
&lt;img alt="Syscall" src="assets/syscall/1545292975_48.png" class="myimg"&gt;&lt;/p&gt;
&lt;p&gt;对于某些非阻塞的系统调用，就不必再与golang运行时交互了，直接调用就可以，这样可以减少两次与golang运行时交互的函数调用开销，这里就掉的是RawSyscall：
&lt;img alt="RawSyscall" src="assets/syscall/1545293027_28.png" class="myimg"&gt;&lt;/p&gt;
&lt;p&gt;网络io操作本来也是阻塞的，但是因为socket fd会被设置为non-blocking，系统调用虽然还是阻塞的系统调用，但是已经不会阻塞调用线程了，所以也无所谓了。&lt;/p&gt;
&lt;p&gt;有个脚本mksyscall.pl根据syscall_linux_amd64.go里面定义的系通调用列表，就是第一张图那些带注释的部分，这个pl脚本会负责生成与之相关的系统调用函数，生成在syscall/zsyscall_linux_amd64.go里面。可以找几个有代表性的来看下生成的系统调用函数：&lt;/p&gt;
&lt;p&gt;比如sendfile是阻塞的系统调用：
&lt;img alt="sendfile" src="assets/syscall/1545293113_49.png" class="myimg"&gt;&lt;/p&gt;
&lt;p&gt;比如settimeofday是非阻塞的系统调用：
&lt;img alt="gettimeofday" src="assets/syscall/1545293136_67.png" class="myimg"&gt;&lt;/p&gt;
&lt;p&gt;epoll相关的epollwait也是阻塞的，但是网络socket fd在go里面都统一设置为了nonblocking fd处理了，因此并不会阻塞。
&lt;img alt="image-20181211095200708" src="assets/syscall/image-20181211095200708-4493120.png" class="myimg"&gt;&lt;/p&gt;
&lt;h2 id="12-开始分析源码"&gt;1.2 开始分析源码 &lt;a href="#12-%e5%bc%80%e5%a7%8b%e5%88%86%e6%9e%90%e6%ba%90%e7%a0%81" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;在讲述系统调用发生的协程调度之前，让我们看看go是如何进入系统调用的，理解了这个让我们不会对后面所说的一些东西感到很陌生。&lt;/p&gt;
&lt;p&gt;golang对操作系统的系统调用作了封装，提供了syscall这样的库让我们执行系统调用。例如，Read系统调用实现如下：&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-go"&gt;func Read(fd int, p []byte) (n int, err error) {
n, err = read(fd, p)
if raceenabled {
if n &amp;gt; 0 {
......
}
......
}
return
}
// 最终封装了Syscall
func read(fd int, p []byte) (n int, err error) {
var _p0 unsafe.Pointer
if len(p) &amp;gt; 0 {
_p0 = unsafe.Pointer(&amp;amp;p[0])
} else {
_p0 = unsafe.Pointer(&amp;amp;_zero)
}
r0, _, e1 := Syscall(SYS_READ, uintptr(fd), uintptr(_p0), uintptr(len(p)))
n = int(r0)
if e1 != 0 {
err = e1
}
return
}
// 我们只关心进入系统调用时调用的runtime·entersyscall
// 和退出时调用的runtime·exitsyscall
TEXT ·Syscall(SB),NOSPLIT,$0-56
CALL runtime·entersyscall(SB)
MOVQ 16(SP), DI
MOVQ 24(SP), SI
MOVQ 32(SP), DX
MOVQ $0, R10 s
MOVQ $0, R8
MOVQ $0, R9
MOVQ 8(SP), AX // syscall entry
SYSCALL
CMPQ AX, $0xfffffffffffff001
JLS ok
MOVQ $-1, 40(SP) // r1
MOVQ $0, 48(SP) // r2
NEGQ AX
MOVQ AX, 56(SP) // errno
CALL runtime·exitsyscall(SB)
RET
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;我们并不关心系统调用到底怎么实现。我们只关心系统调用过程与调度器相关内容，因为Golang自己接管系统调用，调度器便可以在进出系统调用时做一些你所不明白的优化，这里我要带你弄清楚调度器怎么做优化的。&lt;/p&gt;</description></item><item><title>系统调用在不同 CPU 架构上的实现差异</title><link>/blog/2019-07-14-syscall/</link><pubDate>Sun, 14 Jul 2019 00:00:00 +0800</pubDate><guid>/blog/2019-07-14-syscall/</guid><description>&lt;h1 id="背景"&gt;背景 &lt;a href="#%e8%83%8c%e6%99%af" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;p&gt;系统调用是用户态程序请求内核服务的唯一入口。但不同 CPU 架构下，进入系统调用的指令、传参方式各不相同。理解这些差异对于阅读内核源码、进行跨平台开发和性能优化都有帮助。&lt;/p&gt;
&lt;h1 id="正文"&gt;正文 &lt;a href="#%e6%ad%a3%e6%96%87" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="核心问题"&gt;核心问题 &lt;a href="#%e6%a0%b8%e5%bf%83%e9%97%ae%e9%a2%98" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;对于系统调用，每个 CPU 架构都需要解决两个问题：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;strong&gt;用什么指令进入内核态？&lt;/strong&gt;&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;如何传递系统调用号和参数？如何获取返回值？&lt;/strong&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;h2 id="x8632-位"&gt;x86（32 位） &lt;a href="#x8632-%e4%bd%8d" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;机制&lt;/th&gt;
&lt;th&gt;指令&lt;/th&gt;
&lt;th&gt;说明&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;传统方式&lt;/td&gt;
&lt;td&gt;&lt;code&gt;int 0x80&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;通过软中断进入内核，系统调用号存入 &lt;code&gt;eax&lt;/code&gt;，参数通过 &lt;code&gt;ebx&lt;/code&gt;、&lt;code&gt;ecx&lt;/code&gt;、&lt;code&gt;edx&lt;/code&gt;、&lt;code&gt;esi&lt;/code&gt;、&lt;code&gt;edi&lt;/code&gt;、&lt;code&gt;ebp&lt;/code&gt; 传递，返回值存入 &lt;code&gt;eax&lt;/code&gt;&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;快速方式&lt;/td&gt;
&lt;td&gt;&lt;code&gt;sysenter&lt;/code&gt; / &lt;code&gt;sysexit&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;Intel 引入的快速系统调用指令对，比 &lt;code&gt;int 0x80&lt;/code&gt; 更快，无需经过完整的中断处理流程&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;h2 id="x86_6464-位"&gt;x86_64（64 位） &lt;a href="#x86_6464-%e4%bd%8d" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;指令&lt;/th&gt;
&lt;th&gt;说明&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;syscall&lt;/code&gt; / &lt;code&gt;sysret&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;AMD 引入的快速系统调用指令，被 x86_64 Linux 采用&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;参数传递：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;系统调用号：&lt;code&gt;rax&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;参数 1~6：&lt;code&gt;rdi&lt;/code&gt;、&lt;code&gt;rsi&lt;/code&gt;、&lt;code&gt;rdx&lt;/code&gt;、&lt;code&gt;r10&lt;/code&gt;、&lt;code&gt;r8&lt;/code&gt;、&lt;code&gt;r9&lt;/code&gt;&lt;/li&gt;
&lt;li&gt;返回值：&lt;code&gt;rax&lt;/code&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;blockquote&gt;
&lt;p&gt;为何第 4 个参数用 &lt;code&gt;r10&lt;/code&gt; 而非 &lt;code&gt;rcx&lt;/code&gt;？因为 &lt;code&gt;syscall&lt;/code&gt; 指令会自动将 &lt;code&gt;rip&lt;/code&gt; 保存到 &lt;code&gt;rcx&lt;/code&gt;，会破坏 &lt;code&gt;rcx&lt;/code&gt; 的值。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id="arm64"&gt;ARM64 &lt;a href="#arm64" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;指令&lt;/th&gt;
&lt;th&gt;说明&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;svc&lt;/code&gt; (Supervisor Call)&lt;/td&gt;
&lt;td&gt;触发异常进入内核态&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;参数传递：&lt;/p&gt;</description></item></channel></rss>