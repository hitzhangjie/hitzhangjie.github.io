<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Cgo on</title><link>/tags/cgo/</link><description>Recent content in Cgo on</description><generator>Hugo -- gohugo.io</generator><language>en-US</language><lastBuildDate>Sat, 18 Jul 2026 12:00:00 +0800</lastBuildDate><atom:link href="/tags/cgo/index.xml" rel="self" type="application/rss+xml"/><item><title>Go string与C string的区别</title><link>/blog/2019-05-23-gostring_vs_cstring/</link><pubDate>Sat, 18 Jul 2026 12:00:00 +0800</pubDate><guid>/blog/2019-05-23-gostring_vs_cstring/</guid><description>&lt;p&gt;go string和c string是有区别的：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;c string使用&lt;code&gt;'\0'&lt;/code&gt;作为字符串结尾：&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-c"&gt;char *s = “hello\0world&amp;quot;;
printf(&amp;quot;%s\n&amp;quot;, s);
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;只会打印出hello，world是打不出来的，c以\0作为字符串的结束标识。&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;go string只是将&lt;code&gt;'\0'&lt;/code&gt;当做普通的不可点字符，而不是字符串结尾：&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-go"&gt;b := []byte{'h','e','l','l','o','\000','w','o','r','l','d'}
println(string(b))
idx := bytes.IndexByte(b, '\000')
println(idx)
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;会打印出helloworld，而非hello！&lt;/p&gt;
&lt;p&gt;go string的定义可以参考StringHeader结构体的定义：&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-go"&gt;type StringHeader struct {
data unsafe.Pointer
len int
cap int
}
&lt;/code&gt;&lt;/pre&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;另外要注意，golang里面将\0当做是一个八进制数值的开始标识，不管是在字符串中，还是再byte中，如果定义一个字符串&lt;code&gt;s := “hello\0world”&lt;/code&gt;会报错：non-octal character in escape sequence，因为\0wo不是一个有效的八进制数值。&lt;/p&gt;
&lt;p&gt;\0结尾是c spec规定的，go不认，go里面就是StringHeader {data, cap, len}，go里面不可见字符就是不可见字符，没赋予它截断字符串的特殊含义，这么想，感觉go比c设计的要好一点。&lt;/p&gt;</description></item><item><title>Go程序内存泄露问题快速定位</title><link>/blog/2021-04-14-go%E7%A8%8B%E5%BA%8F%E5%86%85%E5%AD%98%E6%B3%84%E9%9C%B2%E9%97%AE%E9%A2%98%E5%BF%AB%E9%80%9F%E5%AE%9A%E4%BD%8D/</link><pubDate>Wed, 14 Apr 2021 18:00:00 +0800</pubDate><guid>/blog/2021-04-14-go%E7%A8%8B%E5%BA%8F%E5%86%85%E5%AD%98%E6%B3%84%E9%9C%B2%E9%97%AE%E9%A2%98%E5%BF%AB%E9%80%9F%E5%AE%9A%E4%BD%8D/</guid><description>&lt;style&gt;
.myimg {
width: 680px;
padding-bottom: 1rem;
}
&lt;/style&gt;
&lt;p&gt;前几天有同学反馈了cgo内存泄露问题，自己也针对这个问题探索了一番，算是为以后解决类似问题提前攒点经验吧。也趁机整理了一下go开发过程中内存泄露问题的一些常用排查方法，也希望对新接触go的同学有所帮助。整理之余，bcc工具之丰富也让我有点惊讶，也希望对自己日后的工作有所帮助吧。&lt;/p&gt;
&lt;h2 id="内存泄漏"&gt;内存泄漏 &lt;a href="#%e5%86%85%e5%ad%98%e6%b3%84%e6%bc%8f" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;内存泄露，一个老生常谈的问题，但即便是老手也会犯一些低级错误。如果没有可靠的研发流程保证在测试阶段发现问题，问题就容易被带到线上。计算资源始终是有限的，问题也不会因为资源充裕就消失不见，产生影响只是时间问题。影响有多大，就要结合场景来说了。&lt;/p&gt;
&lt;p&gt;内存泄漏，最可能的影响就是内存申请失败。但实际上操作系统更聪明，结合系统整体负载情况，它会为每个进程计算一个oom_score，并在内存资源紧张时选择一个合适的进程杀死并回收内存资源，see &lt;a href="https://unix.stackexchange.com/a/153586/95211"&gt;how does the oom killer decide which process to kill first&lt;/a&gt;。&lt;/p&gt;
&lt;p&gt;所以，内存泄露的最终结果，大概率会被操作系统kill，通常进程挂掉后，确认其是否是因为oom问题被kill，可以通过查看 &lt;code&gt;/proc/messages&lt;/code&gt; 来确认是否有对应日志。有的话，那就坐实了oom killed（但是被oom killed的进程不一定意味着存在内存泄露）。&lt;/p&gt;
&lt;h2 id="服务质量"&gt;服务质量 &lt;a href="#%e6%9c%8d%e5%8a%a1%e8%b4%a8%e9%87%8f" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;结合运维手段的变化，来看看是否内存泄漏问题对服务质量造成的影响。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;传统人工方式，通过感知告警、人为介入这种方式，效率低，要十几分钟；&lt;/li&gt;
&lt;li&gt;通过虚拟机自动化部署的方式，感知异常自动重启虚拟机，耗时大约要分钟级；&lt;/li&gt;
&lt;li&gt;通过docker容器化部署的方式，感知异常自动重启容器，耗时大约在秒级；&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;看上去现代运维方式一定程度上可以缓解这个问题，是，这也要分情况：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;如果内存泄露的代码路径不容易被触发，那可能要跑很久才能触发oom kill，如一周；但是如果代码路径在关键代码路径上，且请求量大，频繁触发内存泄露，那可能跑个几分钟就会挂掉；&lt;/li&gt;
&lt;li&gt;跟每次内存泄露的内存大小也有关系，如果泄露的少，多苟活一阵子，反之容易暴毙；&lt;/li&gt;
&lt;li&gt;进程一旦挂掉，这段时间就不能响应了，服务的健康监测、名字服务、负载均衡等措施需要一段时间才能感知到，如果请求量大，服务不可用依然会带来比较大的影响。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;服务质量保证是不变的，所以别管用了什么运维手段，问题终究是问题，也是要解决的。&lt;/p&gt;
&lt;h2 id="go内存泄漏"&gt;Go内存泄漏 &lt;a href="#go%e5%86%85%e5%ad%98%e6%b3%84%e6%bc%8f" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;h3 id="垃圾回收"&gt;垃圾回收 &lt;a href="#%e5%9e%83%e5%9c%be%e5%9b%9e%e6%94%b6" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;自动内存管理减轻了开发人员管理内存的复杂性，不需要像C\C++开发者那样显示malloc、free，或者new、delete。垃圾回收借助于一些垃圾回收算法完成对无用内存的清理，垃圾回收算法有很多，比如：引用计数、标记清除、拷贝、分代等等。&lt;/p&gt;
&lt;p&gt;Go中垃圾回收器采用的是“并发三色标记清除”算法，see:&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;&lt;a href="https://www.ardanlabs.com/blog/2018/12/garbage-collection-in-go-part1-semantics.html"&gt;Garbage Collection In Go : Part I - Semantics&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://www.ardanlabs.com/blog/2019/05/garbage-collection-in-go-part2-gctraces.html"&gt;Garbage Collection In Go : Part II - GC Traces&lt;/a&gt;&lt;/li&gt;
&lt;li&gt;&lt;a href="https://www.ardanlabs.com/blog/2019/07/garbage-collection-in-go-part3-gcpacing.html"&gt;Garbage Collection In Go : Part III - GC Pacing&lt;/a&gt;&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;Go语言支持自动内存管理，那还存在内存泄漏问题吗？&lt;/p&gt;</description></item><item><title>cgo共享库的编译与加载</title><link>/blog/2019-02-23-cgo%E5%85%B1%E4%BA%AB%E5%BA%93/</link><pubDate>Sat, 23 Feb 2019 12:00:00 +0800</pubDate><guid>/blog/2019-02-23-cgo%E5%85%B1%E4%BA%AB%E5%BA%93/</guid><description>&lt;p&gt;go语言提供了c、go互相调用的能力。&lt;/p&gt;
&lt;p&gt;比较常见的是在go中借助c头文件、共享库来调用c代码，通过cgo指令可以指定linkers要连接的共享库的搜索路径。下面是一个示例。&lt;/p&gt;
&lt;p&gt;file: add.h&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-c"&gt;#ifndef _ADD_H_
#define _ADD_H_
int add(int, int);
#endif
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;file: add.c&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-c"&gt;int add(int n, int m)
{
return n + m;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;首先将add.c编译成共享库：&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-bash"&gt;gcc -c -fpic add.c
gcc -shared -o libadd.so add.o
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;file: main.go&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-go"&gt;import &amp;quot;fmt&amp;quot;
/*
# cgo CFLAGS: -I${SRCDIR}
# cgo LDFLAGS: -L${SRCDIR} -ladd
#include &amp;quot;add.h&amp;quot;
*/
import &amp;quot;C&amp;quot;
func main() {
fmt.Printf(&amp;quot;1 + 2 = %v\n&amp;quot;, C.add(1, 2))
}
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;&lt;code&gt;import &amp;quot;C&amp;quot;&lt;/code&gt;必须紧跟在cgo相关的指令、说明后面，它们以注释的形式包括起来，cgo编译器会处理这里的信息。如cgo指令 &lt;code&gt;-I${SRCDIR}&lt;/code&gt;会指导从哪里寻找add.h，&lt;code&gt;cgo -L${SRCDIR}&lt;/code&gt;会指导从哪里寻找libadd.so。&lt;/p&gt;</description></item></channel></rss>