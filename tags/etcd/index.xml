<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Etcd on</title><link>/tags/etcd/</link><description>Recent content in Etcd on</description><generator>Hugo -- gohugo.io</generator><language>en-US</language><lastBuildDate>Fri, 25 Apr 2025 10:00:00 +0800</lastBuildDate><atom:link href="/tags/etcd/index.xml" rel="self" type="application/rss+xml"/><item><title>使用etcd做服务发现的13条教训</title><link>/blog/2025-04-25-%E4%BD%BF%E7%94%A8etcd%E5%81%9A%E6%9C%8D%E5%8A%A1%E5%8F%91%E7%8E%B0%E7%9A%84%E6%95%99%E8%AE%AD/</link><pubDate>Fri, 25 Apr 2025 10:00:00 +0800</pubDate><guid>/blog/2025-04-25-%E4%BD%BF%E7%94%A8etcd%E5%81%9A%E6%9C%8D%E5%8A%A1%E5%8F%91%E7%8E%B0%E7%9A%84%E6%95%99%E8%AE%AD/</guid><description>&lt;h2 id="问题背景"&gt;问题背景 &lt;a href="#%e9%97%ae%e9%a2%98%e8%83%8c%e6%99%af" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;在我们的项目中，我们使用ETCD作为我们的服务注册发现中心，在业务上线前的野蛮生长期，我们大部分时间用在开发产品功能上，对于真正的分布式系统层面可能面临的一些挑战，团队初期并没有花太多时间去打磨。在经历了很多轮的对外CE之后，其实用户量带来的性能层面的挑战极小，与之对应的部署的服务实例数量、机器数量在那个时候也没有构成太多挑战。&lt;/p&gt;
&lt;p&gt;真正的挑战来自产品上线后，大量用户涌入，产品也成为了爆款，惊喜之余也发现了一些隐藏了很久没有被充分重视的性能缺陷，以及潜在的巨大风险点。可以说，在过去的实践中我们曾经犯过不少错误。如果你们也想在ETCD基础上构建可靠的、高可用的服务注册发现能力，你们需要注意避免犯相同的错误。&lt;/p&gt;
&lt;h2 id="实践中的13条教训"&gt;实践中的13条教训 &lt;a href="#%e5%ae%9e%e8%b7%b5%e4%b8%ad%e7%9a%8413%e6%9d%a1%e6%95%99%e8%ae%ad" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;h3 id="mistake-1-部署1个5-members的集群"&gt;Mistake 1: 部署1个5 members的集群 &lt;a href="#mistake-1-%e9%83%a8%e7%bd%b21%e4%b8%aa5-members%e7%9a%84%e9%9b%86%e7%be%a4" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;为了保证ETCD服务的可用性，我们不能像测试环境一样跑一个单节点的ETCD服务，我们至少要部署一个5节点的集群，而且要注意将ETCD members避免部署在同一个母机、机房、可用区。&lt;/p&gt;
&lt;p&gt;ETCD是典型的强一致存储，基于RAFT共识算法来提供保证，一个ETCD集群至少要包含5个members以便leader能至少有3个members投票给它，整个ETCD集群才能正常工作。RAFT算法本身是基于多轮RPC来完成的（AppendEntries、Vote、Heartbeat)，ETCD members之间网络质量首先要好，不能掉链子，这样leader、members之间通信更快速，ETCD服务可用性才更好。&lt;/p&gt;
&lt;p&gt;尽管公司有专业的运维人员来部署、管理、监控ETCD服务，作为开发人员也要知道，要让整个系统的服务注册发现持续可用，ETCD集群应该如何部署。&lt;/p&gt;
&lt;h3 id="mistake-2-给与集群相对充裕的物理资源"&gt;Mistake 2: 给与集群相对充裕的物理资源 &lt;a href="#mistake-2-%e7%bb%99%e4%b8%8e%e9%9b%86%e7%be%a4%e7%9b%b8%e5%af%b9%e5%85%85%e8%a3%95%e7%9a%84%e7%89%a9%e7%90%86%e8%b5%84%e6%ba%90" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;我们有个测试环境ETCD集群，运维同学为了尽量节省成本，部署了一个5节点的集群，但是给的ETCD资源非常紧张，2c4GB来支撑近二十几个测试环境下的服务注册发现，每个测试环境有近100个微服务。另外这套ETCD服务的母机可能存在超卖的情况，ETCD members之间的网络可能也质量比较差……总之就是能省则省，但是问题也随之而来了。&lt;/p&gt;
&lt;p&gt;每隔一段时间我们就会观察到，其管理的测试环境，大量服务开始上报错误日志，ETCD session broken，而且这些服务在watch失败之后会进行大范围的重试注册，尽管注册失败后会打散再次注册的时间，但是由于存在重试风暴，而且ETCD集群本身资源不足，导致重新注册过程经常失败，恶性循环，继续重新注册，继续失败。每次观察到这个情况，只能停掉所有服务，然后每个环境分批次启动。&lt;/p&gt;
&lt;p&gt;这里当然有我们服务注册实现上的不足，这个我们知道，也可以优化。但是你绝对不会在业务快速迭代期，每个月省这几百块钱（8c16g机器云主机只需要几百块)，去浪费一个高级工程师半天或者一天的时间去隔三查无查这些问题。高级工程师早就知道这个地方有问题，但是并不是这个问题现在就非得要解决。&lt;/p&gt;
&lt;h3 id="mistake-3-服务注册引入随机延迟不要扎堆注册"&gt;Mistake 3: 服务注册引入随机延迟+不要扎堆注册 &lt;a href="#mistake-3-%e6%9c%8d%e5%8a%a1%e6%b3%a8%e5%86%8c%e5%bc%95%e5%85%a5%e9%9a%8f%e6%9c%ba%e5%bb%b6%e8%bf%9f%e4%b8%8d%e8%a6%81%e6%89%8e%e5%a0%86%e6%b3%a8%e5%86%8c" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;对于需要蓝绿部署的业务，你会准备两个集群轮换使用。比如，游戏业务中会有大版本更新的诉求，每个版本内容更新非常大，通常客户端版本和服务器版本要完全一致，每当发布新版本时有些支持不停服更新的游戏业务就会重新拉起一个新的集群，老版本的玩家逐渐更新迁移到新版本后，再将老集群停掉。&lt;/p&gt;
&lt;p&gt;注意在起服的瞬间，涉及到大量服务的重新启动，比如我们的业务大约有10000多个节点需要新拉起。由于我们服务注册、发现上存在实现不健壮的地方，导致运维拉起新集群时，一瞬间存在大量微服务实例像ETCD集群发起注册，注册过程包括：1）getbyprefix &lt;code&gt;/lobby-${cluster}/&lt;/code&gt;，2）put &lt;code&gt;/lobby-${cluster}/${service}/${zone}/${instanceNo}，&lt;/code&gt;3）watch &lt;code&gt;/lobby-${cluster}，4）如果期间遇到失败会重试1)~3）这个过程。&lt;/code&gt;&lt;/p&gt;
&lt;p&gt;框架层实现不健壮的地方，对etcd server造成了不必要的开销：&lt;/p&gt;
&lt;p&gt;1）getbyprefix的前缀太短，导致没有按需拉取需要调用的服务的列表，返回数据量过大，&lt;/p&gt;
&lt;p&gt;2）有新节点注册时，watcher监听的前缀太短，导致etcd存储层一个更新事件需要通知大量实际上不需要通知的节点。&lt;/p&gt;
&lt;p&gt;框架层实现不健壮的地方，导致对进程本身造成了不必要的开销：&lt;/p&gt;
&lt;p&gt;1）getbyprefix的前缀太短，拉取了大量当前进程不需要的服务列表；&lt;/p&gt;
&lt;p&gt;2）更新本地hashring时（一致性hash方式)，对于相同服务下的实例列表构架hashring时不是一次性完成构建，而是每个节点更新一次；&lt;/p&gt;
&lt;p&gt;这几个问题凑在一起，简直是典型的错误都犯了，但是瞬间也让主程冷静了，早就提醒过嘛，说它不行它就是不行。最终还是被运维的无心操作给触发了，原本是分批次启动 ，那一次运维重启了战斗服所有服务实例，大约4k+实例。&lt;/p&gt;
&lt;p&gt;尽管这个问题是几个问题都犯了导致的，但是每个问题独立去看依然很有警示意义，应该避免。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;ps: 这种情况下化解这个问题，先分批启动服务降低ETCD server负载，让其他服务可以正常完成注册；如果节点太多时，服务本身拉取、更新hashring耗时太久（确实太久了)，此时先把注册耗时加长，从2s加到10s加到15s最终加到30s，先拉起来再说。后续push的开销主要在ETCD server端，对客户端影响不大。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h3 id="mistake-4-getbyprefix-by-pages避免一次性取大量数据"&gt;Mistake 4: getbyprefix by pages+避免一次性取大量数据 &lt;a href="#mistake-4-getbyprefix-by-pages%e9%81%bf%e5%85%8d%e4%b8%80%e6%ac%a1%e6%80%a7%e5%8f%96%e5%a4%a7%e9%87%8f%e6%95%b0%e6%8d%ae" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;ETCD getbyprefix操作，是支持分页拉取的，你可以通过 &lt;code&gt;WithLimit(...)&lt;/code&gt;以及其他Option来实现分页拉取。这一定程度上可以缓解一次性大量拉取对ETCD Server造成的压力。比如某个service下的实例数非常多，我们游戏战斗服dsa服务高峰期有6k+，每个dsa启动时当时还watch了其他dsa服务，所以每个dsa进程服务注册发现时都需要拉取大量dsa实例列表，启动的早的dsa进程getbyprefix少拉取点（后面注册上的通过watch机制通知)，启动的越慢的要拉取的就更多（通过watch机制通知的就少)。&lt;/p&gt;
&lt;p&gt;后续我们去掉了dsa进程之间的互相watch，规避了这个问题。当时我们的情景依然是几个问题一起犯了导致的，不该watch的watch了，涉及到大量信息时又不分页拉取导致etcdserver负载也高，然后拉回来大量节点还不注意批处理的方式更新本地hashring，还对拉回来的节点列表逐个处理。没错，每遍历到一个节点，就更新一次hashring，造成本地进程CPU开销也高，6k个节点更新hashring就花了15~20s左右，导致我们不得不将etcd服务注册流程总耗时从2s逐渐增加到了30s。&lt;/p&gt;
&lt;p&gt;如果你的getbyprefix前缀太宽泛，一次性拉取的实例数量过多，你就应该从中得到警示，避免给ETCD server造成瞬间太大的压力，如果你的集群很大，这个压力就不容小觑，可能导致ETCD负载过高。而如果ETCD负载过高，影响到了集群内部的心跳机制可能导致触发重新选主，这期间如果有其他ETCD client访问集群会返回NoLead error，通常会导致ETCD client keepalive channel失败，必须重新开始注册。而已经注册到ETCD集群的节点，也会错误认为该该节点已经被DELETE掉了，有可能会认为进程已经不可用。&lt;/p&gt;
&lt;p&gt;Anyway，那些看上去简单到可以忽略的问题，在集群规模变大后，可能就不能再被简单的忽略。&lt;/p&gt;
&lt;h3 id="mistake-5-getbyprefixwatch真正关心的keys避免拉取收取太多keys及事件"&gt;Mistake 5: getbyprefix/watch真正关心的keys+避免拉取/收取太多keys及事件 &lt;a href="#mistake-5-getbyprefixwatch%e7%9c%9f%e6%ad%a3%e5%85%b3%e5%bf%83%e7%9a%84keys%e9%81%bf%e5%85%8d%e6%8b%89%e5%8f%96%e6%94%b6%e5%8f%96%e5%a4%aa%e5%a4%9akeys%e5%8f%8a%e4%ba%8b%e4%bb%b6" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;正如Mistake 3中所提及的，服务注册发现的情景下，我们应该get/watch我们关心的prefix，不要为了省事将这个prefix范围给扩大，总有一天集群规模扩大到一定数量时，我们会有招架不住的一天，那时就是非常大的代价。可能你要分批次处理才能恢复服务的全局可用。通常这样的代价没有一个项目负责人会接受的。而且还是在明知可以避免的情况下。明知故犯，比不知道，还可恶。&lt;/p&gt;</description></item><item><title>如何使用raft算法开发强一致kv存储系统</title><link>/blog/2021-09-04-%E5%A6%82%E4%BD%95%E4%BD%BF%E7%94%A8raft%E5%BC%80%E5%8F%91%E5%BC%BA%E4%B8%80%E8%87%B4kv%E5%AD%98%E5%82%A8%E7%B3%BB%E7%BB%9F/</link><pubDate>Sun, 05 Sep 2021 00:47:15 +0800</pubDate><guid>/blog/2021-09-04-%E5%A6%82%E4%BD%95%E4%BD%BF%E7%94%A8raft%E5%BC%80%E5%8F%91%E5%BC%BA%E4%B8%80%E8%87%B4kv%E5%AD%98%E5%82%A8%E7%B3%BB%E7%BB%9F/</guid><description>&lt;h2 id="本文内容"&gt;本文内容 &lt;a href="#%e6%9c%ac%e6%96%87%e5%86%85%e5%ae%b9" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;本文结合etcd源码来进行介绍，etcd/contrib/raftexample提供了一个基于etcd/raft实现的kv存储系统。从该示例出发，我们来看一看如何基于raft算法开发一个强一致的kv存储系统。&lt;/p&gt;
&lt;p&gt;看完本文的源码分析后，上手一个raft强一致系统开发就不是什么难事了。&lt;/p&gt;
&lt;p&gt;ps：假定读者已经阅读并理解了raft论文，这里有我的&lt;a href="https://github.com/hitzhangjie/distributed-system-series/blob/master/papers/In%20Search%20of%20an%20Understandable%20Consensus%20Algorithm%20(Extended%20Version).pdf"&gt;批注版的In Search of an Understandable Consensus Algorithm.pdf&lt;/a&gt;，读起来可能会好理解点。&lt;/p&gt;
&lt;h2 id="etcdraft"&gt;etcd/raft &lt;a href="#etcdraft" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;etcd服务端程序入口：see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/server/main.go"&gt;源码&lt;/a&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;启动过程中区分当前节点类型：根据data-dir目录下的目录名member/proxy/empty来区分，然后启动etcd实例或者proxy；&lt;/li&gt;
&lt;li&gt;启动etcd服务节点：startEtcd这个函数，逻辑主要包括启动供集群节点间通信的rafthttp服务，以及供客户端请求的服务；&lt;/li&gt;
&lt;li&gt;启动etcd proxy：startProxy这个函数，逻辑主要是启动etcd代理；&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;etcd哪些部分值得学习：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;etcd proxy从项目功能上来说虽然很重要，但是从学习角度来说没那么有价值，不看这个；&lt;/li&gt;
&lt;li&gt;etcd server从项目功能上来说是核心，但是我们也没有必要学习所有的请求处理逻辑，重点是关注读写操作时如何基于raft实现强一致；&lt;/li&gt;
&lt;li&gt;raft：这部分是raft算法的核心实现，从理解raft论文到算法工程化需要额外做出巨大的优化，这些知识点往往是通用的，重点掌握；&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;raft部分：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;pb协议：
&lt;ul&gt;
&lt;li&gt;raft peers的通信协议，see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/raft/raftpb/raft.proto"&gt;源码&lt;/a&gt;；&lt;/li&gt;
&lt;li&gt;raft算法中提到核心的几个rpc就是Vote、AppendEntries，但是工程中需要考虑更多，详见上述pb中的enum MessageType；&lt;/li&gt;
&lt;li&gt;上述pb中的message Message类型定义了rpc通信过程中的请求/响应，不同rpc通过MessageType type字段区分；&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;li&gt;状态机：
&lt;ul&gt;
&lt;li&gt;raft实现数据一致性是通过replicated log（复制日志）实现的，这里的replicated log有时也称为WAL（write ahead log）；&lt;/li&gt;
&lt;li&gt;raft算法中，每个节点raftnode可能处于以下状态中的一种：follower、candidate、precandidate、leader；&lt;/li&gt;
&lt;li&gt;raft算法中，每个节点的状态可以通过一个状态机来建模；&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;了解了这些基础知识之后，我们结合etcd/contrib/raftexample来解释下raft如何选举，以及leader遇到写操作如何保证数据强一致。&lt;/p&gt;
&lt;h2 id="etcdraft如何进行leader选举"&gt;etcd/raft如何进行leader选举 &lt;a href="#etcdraft%e5%a6%82%e4%bd%95%e8%bf%9b%e8%a1%8cleader%e9%80%89%e4%b8%be" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;newRaftNode&lt;/strong&gt;
newRaftNode，see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/contrib/raftexample/raft.go?L87:6"&gt;源码&lt;/a&gt;，这个函数包括创建一个var rc raftNode，然后rc.startRaft()，这个函数包含非常重要的几个部分：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;startNode，see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/raft/node.go?L218:6"&gt;源码&lt;/a&gt;，这个主要是建立好raftnode启动时的一些初始状态转换，有一个for事件循环处理，如改变raftnode的状态：tick函数、step函数，以及一些message的处理等等；&lt;/li&gt;
&lt;li&gt;serveRaft：&lt;/li&gt;
&lt;li&gt;serveChannels：see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/contrib/raftexample/raft.go?L402"&gt;源码&lt;/a&gt;&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;startNode&lt;/strong&gt;:&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;如何查看这部分源码呢，首先从启动一个raftnode开始吧：see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/raft/node.go?L214:4"&gt;源码&lt;/a&gt;；&lt;/li&gt;
&lt;li&gt;StartNode函数启动一个raftnode，节点刚启动的时候state都是follower：see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/raft/raft.go?L360"&gt;源码&lt;/a&gt;；&lt;/li&gt;
&lt;li&gt;StartNode→Bootstrap(peers)通过配置告诉当前raftnode有多少个raftpeers，然后这些raftpeers加入与当前节点所在的集群属于变更配置，也要记录到raftlog中；&lt;/li&gt;
&lt;li&gt;raftnode真正跑起来是在这里：see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/raft/node.go?L303:16"&gt;源码&lt;/a&gt;，这里有个大的for循环，node的主要逻辑都在这里了；
&lt;ul&gt;
&lt;li&gt;tickElection：for/switch-case n.tickC，选举逻辑，此时如果当前raftnode为follower或者candidate吧，此时的tick函数为tickElection，如果选举超时时间过了并且没收到leader的heartbeat来重置选举超时时间，此时会将MsgHup消息类型传入step函数中，将当前follower变为candidate发起选举：see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/raft/raft.go?L645"&gt;源码&lt;/a&gt;。这里的选举在raft论文中是直接就是选举动作，但是工程上做了优化，引入了一个可选的两阶段选举prevote。虽然可以tick是触发了tickElection，但是这个后续执行中会检查当前节点是否有资格成为leader，不一定有资格（比如自身的WAL不满足条件）。&lt;/li&gt;
&lt;li&gt;假如有资格发起选举，则会调用becomeCandidate，会将当前raftnode的term+1，并且step函数变为stepCandidate。然后会调用r.poll来判断是否胜选：see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/raft/raft.go?L803"&gt;源码&lt;/a&gt;，其实这里是判断的自己给自己投票的话能否胜选，对于single raftnode的集群有用，假如是多节点集群那么这里无法胜选，继续看。ps：如果胜选就becomeLeader成为leader了。如果不是单节点，就要通过r.send发送投票给各个peers：see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/raft/raft.go?L833"&gt;源码&lt;/a&gt;。这里的r.send并不是真的网络发送，而是记录到r.msgs里面等下处理这里的r.msgs。注意这里r.send的时候已经编程了MsgVote类型了，表示投票请求，后续也应该收到MsgVoteResp。&lt;/li&gt;
&lt;li&gt;r.msgs什么时候处理呢？还是前面我们提到的这个大循环体，每轮循环都会检查r.msgs中有没有message要处理：see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/raft/node.go?L316"&gt;源码&lt;/a&gt;，这里的函数n.rn.HasReady()方法检查到len(r.msgs)&amp;gt;0，则认为有消息要处理，这个消息最终会被包装到一个Ready{}事件中，这个事件会被丢到n.readyc这个chan中，什么时候处理在下面serveChannels中介绍。&lt;/li&gt;
&lt;li&gt;becomeLeader：see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/raft/raft.go?L724"&gt;源码&lt;/a&gt;，step函数变为stepLeader，tick函数则变成tickHeartbeat，意味着当前为leader需要给followers定时发送heartbeat来重置它们的选举超时时间，那么heartbeat是什么形式的呢？其实就是通过appendEntry，只不过entry为空，用这种空的entry来表示心跳。leader就要担负起write请求的重任了。&lt;/li&gt;
&lt;li&gt;但是如果没胜选的话，raftnode的状态就是candidate，step函数未stepCandidate，下面会继续用到。&lt;/li&gt;
&lt;/ul&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;&lt;strong&gt;serveChannels&lt;/strong&gt;：
前面关于r.msgs的消息没跟踪到在哪里处理的，我们看下是不是在serveChannels里面？
serveChannels: see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/contrib/raftexample/raft.go?L402"&gt;源码&lt;/a&gt;&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;这个函数里面也有一个for事件循环，当它发现rc.node.Ready()有var rd Ready{}事件可处理时，如果rd上有非空的snapshot，就写入storage，然后将rd.Entries也记录到rd.HardState，然后将rd.Entries也写入storage，最后将rd.Messages发送到peers。我们感觉voteMsg是在这个时候发送给peers的，到底是不是呢：see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/contrib/raftexample/raft.go?L459:4"&gt;源码&lt;/a&gt;。是的，这里的rd事件就是从raftnode.Ready()从发从其raftnode.readyc这个channel中取出来的。取出来后通过transport发送出去，这样voteMsg就发送出去了，那么投票的响应又是什么时候收到、什么时候处理的呢？&lt;/li&gt;
&lt;li&gt;startRaft/AddPeer的时候会调用startPeer，内部会开始循环收包，收peer发来的raftmessage放入一个recvc chan中，startPeer中专门开启了一个goroutine来检查recvc中有没有peer发送来的消息，比如peer发送给我们的voteMsg的响应包：see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/server/etcdserver/api/rafthttp/peer.go?L177"&gt;源码&lt;/a&gt;。这里通过raft.Process(ctx,m)对raftmessage进行处理。如何处理是在这个示例代码中定义的：see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/contrib/raftexample/raft.go?L499"&gt;源码&lt;/a&gt;，即调用step函数进行处理。我们再往回看下，发送这个消息前已经把节点的step函数修改为了stepCandidate，那我们再看下这个函数里面干了啥，猜测应该有判断是否收到多数投票确认的逻辑；&lt;/li&gt;
&lt;li&gt;stepCandidate：see &lt;a href="https://sourcegraph.com/github.com/etcd-io/etcd/-/blob/raft/raft.go?L1399:7"&gt;源码&lt;/a&gt;，我们不考虑可选的prevote阶段，很显然这个消息MsgVote的响应类型应该是MsgVoteResp，如果是的话，就继续r.poll检查下是否胜选吧，如果胜选了，则自己becomeLeader，然后广播appendEntries，这里append是干啥，是为了通治其他peers更新commit index吧。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这样leader选举就完成了！！！&lt;/p&gt;</description></item></channel></rss>