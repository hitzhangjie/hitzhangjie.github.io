<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Signal on</title><link>/tags/signal/</link><description>Recent content in Signal on</description><generator>Hugo -- gohugo.io</generator><language>en-US</language><lastBuildDate>Mon, 26 May 2025 00:00:00 +0800</lastBuildDate><atom:link href="/tags/signal/index.xml" rel="self" type="application/rss+xml"/><item><title>Go 信号处理：同步信号与异步信号的区别</title><link>/blog/2025-05-26-go-signals/</link><pubDate>Mon, 26 May 2025 00:00:00 +0800</pubDate><guid>/blog/2025-05-26-go-signals/</guid><description>&lt;p&gt;Go 程序运行时经常会收到操作系统的信号，理解同步信号和异步信号的区别，对于编写健壮的程序非常重要。&lt;/p&gt;
&lt;h2 id="1-同步信号synchronous-signal"&gt;1. 同步信号（Synchronous Signal） &lt;a href="#1-%e5%90%8c%e6%ad%a5%e4%bf%a1%e5%8f%b7synchronous-signal" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;同步信号由程序自身的执行直接触发，与特定指令的执行直接相关，在指令执行过程中立即发生。&lt;/p&gt;
&lt;p&gt;常见的同步信号包括：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;SIGSEGV&lt;/strong&gt;（段错误）：访问非法内存地址&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;SIGFPE&lt;/strong&gt;（浮点异常）：除以零等算术错误&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;SIGILL&lt;/strong&gt;（非法指令）：执行了无效的机器指令&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这些信号不能被普通的信号处理机制捕获，因为它们在指令执行过程中就发生了。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;并不是完全不能捕获——比如 Go runtime 就捕获并处理了这些信号，但普通用户不应自行捕获处理，因为处理不当可能导致更严重的问题。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id="2-异步信号asynchronous-signal"&gt;2. 异步信号（Asynchronous Signal） &lt;a href="#2-%e5%bc%82%e6%ad%a5%e4%bf%a1%e5%8f%b7asynchronous-signal" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;异步信号由外部事件触发，与程序当前执行状态无关，可以在任何时候发生。&lt;/p&gt;
&lt;p&gt;常见的异步信号包括：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;SIGINT&lt;/strong&gt;（中断信号）：通常由 Ctrl+C 触发&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;SIGTERM&lt;/strong&gt;（终止信号）：请求程序终止&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;SIGUSR1/SIGUSR2&lt;/strong&gt;（用户自定义信号）&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;这些信号可以被 &lt;code&gt;signal.Notify&lt;/code&gt; 捕获和处理。&lt;/p&gt;
&lt;h2 id="3-举例说明"&gt;3. 举例说明 &lt;a href="#3-%e4%b8%be%e4%be%8b%e8%af%b4%e6%98%8e" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;pre&gt;&lt;code class="language-go"&gt;// 异步信号可以被捕获
c := make(chan os.Signal, 1)
signal.Notify(c, syscall.SIGINT) // 可以捕获 Ctrl+C
// 同步信号不能被普通方式捕获
var s *struct{ Age int }
println(s.Age) // 触发 SIGSEGV，不能被 signal.Notify 捕获
&lt;/code&gt;&lt;/pre&gt;
&lt;h2 id="4-为什么会有这种区别"&gt;4. 为什么会有这种区别 &lt;a href="#4-%e4%b8%ba%e4%bb%80%e4%b9%88%e4%bc%9a%e6%9c%89%e8%bf%99%e7%a7%8d%e5%8c%ba%e5%88%ab" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;ul&gt;
&lt;li&gt;同步信号表示程序执行出现了严重错误&lt;/li&gt;
&lt;li&gt;这些错误通常意味着程序处于一个不可恢复的状态&lt;/li&gt;
&lt;li&gt;允许程序处理这些信号可能会导致更多的问题&lt;/li&gt;
&lt;li&gt;操作系统选择直接终止程序，而不是给程序处理的机会&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="5-实际应用中的处理方式"&gt;5. 实际应用中的处理方式 &lt;a href="#5-%e5%ae%9e%e9%99%85%e5%ba%94%e7%94%a8%e4%b8%ad%e7%9a%84%e5%a4%84%e7%90%86%e6%96%b9%e5%bc%8f" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;&lt;strong&gt;对于同步信号（如 SIGSEGV）：&lt;/strong&gt;&lt;/p&gt;</description></item><item><title>go程序信号处理过程</title><link>/blog/2021-05-25-%E8%BF%90%E8%A1%8C%E6%97%B6_go%E7%A8%8B%E5%BA%8F%E4%BF%A1%E5%8F%B7%E5%A4%84%E7%90%86%E8%BF%87%E7%A8%8B/</link><pubDate>Tue, 25 May 2021 11:32:39 +0800</pubDate><guid>/blog/2021-05-25-%E8%BF%90%E8%A1%8C%E6%97%B6_go%E7%A8%8B%E5%BA%8F%E4%BF%A1%E5%8F%B7%E5%A4%84%E7%90%86%E8%BF%87%E7%A8%8B/</guid><description>&lt;h2 id="go信号处理基础"&gt;go信号处理基础 &lt;a href="#go%e4%bf%a1%e5%8f%b7%e5%a4%84%e7%90%86%e5%9f%ba%e7%a1%80" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;go os.signal package对信号处理做了封装，其中信号SIGKILL、SIGSTOP是操作系统规定的不允许捕获的信号，是不受os.signal这个package影响的&lt;/p&gt;
&lt;p&gt;go中将信号分为两类：同步信号和异步信号。&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;同步信号：指的是go程序运行时程序内部错误触发的一些问题，如SIGBUS、SIGFPE、SIGSEGV，这些信号会被转换成运行时panic信息；&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;异步信号：除了上述提及的信号之外的信号，就是异步信号了。异步信号不是程序内部错误导致的，而是由操作系统或者外部其他程序发送给它的。&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="有哪些异步信号"&gt;有哪些异步信号？ &lt;a href="#%e6%9c%89%e5%93%aa%e4%ba%9b%e5%bc%82%e6%ad%a5%e4%bf%a1%e5%8f%b7" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;ul&gt;
&lt;li&gt;当程序失去对控制终端的控制时，会收到SIGHUP信号；&lt;/li&gt;
&lt;li&gt;在控制终端中输入Ctrl+C时会收到SIGINT信号；&lt;/li&gt;
&lt;li&gt;在控制终端中输入Ctrl+\时会受到SIGQUIT信号；&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;ps：通常想让程序退出的话，Ctrl+C就可以了，如果想让程序退出同时打印栈转储信息，那就用Ctrl+\。&lt;/p&gt;
&lt;h3 id="默认的信号处理方式"&gt;默认的信号处理方式？ &lt;a href="#%e9%bb%98%e8%ae%a4%e7%9a%84%e4%bf%a1%e5%8f%b7%e5%a4%84%e7%90%86%e6%96%b9%e5%bc%8f" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;接收到信号之后，肯定有默认的处理方式，这个在学习linux信号处理时肯定有了解过的，在go程序中可能只是默认处理方式有点不同，这个有需要的时候去了解就可以了。这里不展开了。&lt;/p&gt;
&lt;p&gt;值得一提的是信号SIGPROF，这个信号用于实现runtime.CPUProfile。&lt;/p&gt;
&lt;h3 id="自定义信号处理方式"&gt;自定义信号处理方式？ &lt;a href="#%e8%87%aa%e5%ae%9a%e4%b9%89%e4%bf%a1%e5%8f%b7%e5%a4%84%e7%90%86%e6%96%b9%e5%bc%8f" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;自定义信号处理方式，在linux signal函数中可以指定信号及对应对应的处理函数，go中类似，它允许通过os.Notify指定一个或多个信号chan，里面可以注册感兴趣的信号，当收到这些信号时，就可以执行用户自定义的信号处理逻辑。&lt;/p&gt;
&lt;h3 id="sigpipe信号处理"&gt;SIGPIPE信号处理 &lt;a href="#sigpipe%e4%bf%a1%e5%8f%b7%e5%a4%84%e7%90%86" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;当程序write broken pipe时，会收到SIGPIPE信号，比如写网络连接失败，如果不做处理默认崩溃掉那就完蛋了。go程序中对这个做了优化处理。&lt;/p&gt;
&lt;p&gt;write broken pipe的行为与write的file descriptor的fd有关系：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;如果fd是stdout、stderr，那么程序收到SIGPIPE信号，默认行为是程序会退出；&lt;/li&gt;
&lt;li&gt;如果是其他fd，程序收到SIGPIPE信号，默认行为是不采取任何动作，对应的write操作返回一个EPIPE错误；&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;ps：后者很重要，写网络连接失败是常有的事情，linux c程序如果不显示处理SIGPIPE信号，默认行为将是程序直接crash，go程序对此作了优化，让write返回error而非crash，对于go将构建高性能、稳定健壮的网络程序的初衷来说是有必要的。&lt;/p&gt;
&lt;h3 id="cgo程序信号处理"&gt;cgo程序信号处理？ &lt;a href="#cgo%e7%a8%8b%e5%ba%8f%e4%bf%a1%e5%8f%b7%e5%a4%84%e7%90%86" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;涉及到cgo就要分几种情况来讨论，这里会有点麻烦了，涉及到信号处理函数的重复注册、信号掩码设置、信号处理函数的栈等问题，在os/signal/doc.go里面有这方面的描述，这里不赘述。&lt;/p&gt;
&lt;h2 id="go信号处理过程"&gt;go信号处理过程 &lt;a href="#go%e4%bf%a1%e5%8f%b7%e5%a4%84%e7%90%86%e8%bf%87%e7%a8%8b" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;介绍了go程序内部的信号处理过程。GMP调度模型里面，每个M都有一个独立的gsignal goroutine，系统投递信号给进程时实际上是有gsignal goroutine来接受这个信号，然后检查下是否可处理。如果可处理就将其push到一个信号队列中，然后有一个专门的goroutine执行signal.loop，这个函数从上述信号队列中取信号，并转移到用户自定义的chan os.Signal中，再由我们自己写的chan read代码消费，并执行处理。&lt;/p&gt;
&lt;p&gt;对应到源码中主要有几个函数：&lt;/p&gt;
&lt;p&gt;对应到源码中主要有几个函数：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;
&lt;p&gt;os/signal/signal.go：这个函数里面在func init()的时候有启动一个loop函数，这个函数内调用runtime.signal_recv来不停地接收信号，然后检查程序通过os.Notify为哪些chan os.Signal订阅了该信号，就将该信号push到对应的chan中，后面应用程序就可以自行处理了；&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;runtime/sigqueue.go：runtime.sigsend、runtime.signal_recv这两个函数很重要，前者是程序收到系统发送来的信号时将信号写入outgoing sigqueue中，其实就是sig结构体的mask字段，后面signal_recv的时候也是从该mask字段读取，并写入recv字段中，recv中非0的应该就是表示收到了信号（信号编号为索引值）；&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;runtime/signal_unix.go：有个函数sighandler，这个函数负责对不同的信号执行不同的处理，比如抢占式调度SIGURG的处理，比如SIGPROF的处理，比如我们这里讨论的一些异步信号的处理sigsend。在go程序中不管是什么信号，这些信号是在sighandler做不同处理。sighandler虽然名字是信号处理函数，我们也看到了通过setsig将所有信号全部设置sighandler为信号处理函数，但是其实这只是表现。setsig函数内部又做了一个转换，将信号的信号处理函数设置为了sigtramp活着cgosigtramp，这些函数内部又调用sighandler。下面会提到sigtramp的逻辑；&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;runtime/runtime2.go：这里定义了GMP调度模型中的m，m包含一个成员gsignal，它表示信号处理用的goroutine。os_linux.go中mpreinit会为创建一个goroutine，协程栈被初始化一个32KB大小的信号处理栈，很大这是为了兼容不同操作系统的一些问题，linux要≥2KB，OSX要≥8KB&amp;hellip;&lt;/p&gt;
&lt;/li&gt;
&lt;li&gt;
&lt;p&gt;sigtramp是注册到操作系统的信号处理函数，当操作系统执行系统调用返回时检查进程有没有信号到达，有并且没有屏蔽信号则执行对应的信号处理函数，这个时候是切到了用户态去执行信号处理函数。在执行信号处理函数的时候比较特殊，go需要为信号处理函数准备一个不同的栈帧，即信号处理栈，这个前面提过了是一个32KB大小的栈，然后将当前m.g设置为gsignal（栈大小为32KB），栈准备好之后，执行前面提过的sighandler执行信号处理，处理完成返回后，再将m.g设置为原来的g恢复正常执行。其实signhandler执行过程中，sigsend发送到outgoing sigqueue，然后signal_recv收信号发送到os.Notify订阅的chan，就完事了，后面就是我们熟悉的chan read并处理逻辑了。&lt;/p&gt;
&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;算是介绍的比较详细了吧，篇幅、时间原因就不贴源码了，感兴趣的可以对着提及的源码仔细看看、求证一下。&lt;/p&gt;</description></item><item><title>中断与信号的区别</title><link>/blog/2018-09-06-interrupt-vs-signal/</link><pubDate>Thu, 06 Sep 2018 00:00:00 +0800</pubDate><guid>/blog/2018-09-06-interrupt-vs-signal/</guid><description>&lt;h1 id="背景"&gt;背景 &lt;a href="#%e8%83%8c%e6%99%af" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;p&gt;很多人容易混淆 Linux 中的&amp;quot;信号（signal）&amp;ldquo;和&amp;quot;中断（interrupt）&amp;quot;，甚至认为信号是通过软中断实现的。本文从内核实现的角度澄清这两者的关系。&lt;/p&gt;
&lt;h1 id="正文"&gt;正文 &lt;a href="#%e6%ad%a3%e6%96%87" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="信号不是通过中断实现的"&gt;信号不是通过中断实现的 &lt;a href="#%e4%bf%a1%e5%8f%b7%e4%b8%8d%e6%98%af%e9%80%9a%e8%bf%87%e4%b8%ad%e6%96%ad%e5%ae%9e%e7%8e%b0%e7%9a%84" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;Linux 的信号是在进程的进程描述符（&lt;code&gt;task_struct&lt;/code&gt;）中维护了一个信号队列，用于接收发送给该进程的信号。&lt;/p&gt;
&lt;p&gt;当进程被调度时，调度器会检查进程是否有未处理的不可屏蔽信号。如果有，内核会在进程返回用户态之前执行信号对应的信号处理函数。&lt;strong&gt;这个过程根本不是什么软中断&lt;/strong&gt;，它是内核在正常的&amp;quot;返回到用户态&amp;quot;路径上的检查。&lt;/p&gt;
&lt;h2 id="中断的分类"&gt;中断的分类 &lt;a href="#%e4%b8%ad%e6%96%ad%e7%9a%84%e5%88%86%e7%b1%bb" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;h3 id="硬件中断"&gt;硬件中断 &lt;a href="#%e7%a1%ac%e4%bb%b6%e4%b8%ad%e6%96%ad" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;由设备控制器向 CPU 引脚发送中断请求：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;可屏蔽中断（Maskable Interrupt）&lt;/strong&gt;：通过 INTR 引脚发送，CPU 可以通过清除 EFLAGS 中的 IF 位来暂时屏蔽。绝大多数外设中断都属此类。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;不可屏蔽中断（Non-Maskable Interrupt, NMI）&lt;/strong&gt;：通过 NMI 引脚发送，CPU 无法屏蔽。通常用于极其紧急的事件，如硬件内存错误、看门狗超时。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="异常exception"&gt;异常（Exception） &lt;a href="#%e5%bc%82%e5%b8%b8exception" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;由 CPU 自身在执行指令过程中检测到的错误条件触发：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;&lt;strong&gt;Fault&lt;/strong&gt;：可纠正的异常，如缺页异常（page fault）。CPU 保存的返回地址是触发异常的指令地址，处理完后重新执行该指令。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Trap&lt;/strong&gt;：主动触发的异常，如调试断点（&lt;code&gt;int3&lt;/code&gt;）。返回地址是下一条指令地址。&lt;/li&gt;
&lt;li&gt;&lt;strong&gt;Abort&lt;/strong&gt;：不可恢复的错误，如硬件错误。不保存可靠的返回地址。&lt;/li&gt;
&lt;/ul&gt;
&lt;h3 id="软中断software-interrupt"&gt;软中断（Software Interrupt） &lt;a href="#%e8%bd%af%e4%b8%ad%e6%96%adsoftware-interrupt" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h3&gt;
&lt;p&gt;软中断是通过 &lt;code&gt;int&lt;/code&gt; 指令由软件主动触发的，不是由设备控制器发出的。&lt;/p&gt;
&lt;blockquote&gt;
&lt;p&gt;常见的例子是 &lt;code&gt;int 0x80&lt;/code&gt; —— 在 x86 早期，Linux 使用 &lt;code&gt;int 0x80&lt;/code&gt; 作为系统调用的入口（已被 &lt;code&gt;sysenter&lt;/code&gt;/&lt;code&gt;syscall&lt;/code&gt; 取代）。注意这里的中断向量已经不是 BIOS 初始化的那个了，Linux 内核初始化时已经重新设置了中断向量表（IDT）。&lt;/p&gt;
&lt;/blockquote&gt;
&lt;h2 id="中断向量号分配"&gt;中断向量号分配 &lt;a href="#%e4%b8%ad%e6%96%ad%e5%90%91%e9%87%8f%e5%8f%b7%e5%88%86%e9%85%8d" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;x86 体系下，中断向量号的分配大致如下：&lt;/p&gt;</description></item><item><title>文件锁阻塞与信号响应</title><link>/blog/2017-11-20-filelock-signal/</link><pubDate>Sat, 01 Jan 2000 00:00:00 +0800</pubDate><guid>/blog/2017-11-20-filelock-signal/</guid><description>&lt;h1 id="背景"&gt;背景 &lt;a href="#%e8%83%8c%e6%99%af" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;p&gt;文件锁（&lt;code&gt;flock&lt;/code&gt;/&lt;code&gt;fcntl&lt;/code&gt;）是进程间同步的常用手段。当一个进程尝试获取已被占用的文件锁时，默认行为是阻塞等待。但问题来了：&lt;strong&gt;此时进程还能响应信号（如 &lt;code&gt;Ctrl+C&lt;/code&gt; 发送的 &lt;code&gt;SIGINT&lt;/code&gt;）吗？&lt;/strong&gt;&lt;/p&gt;
&lt;h1 id="正文"&gt;正文 &lt;a href="#%e6%ad%a3%e6%96%87" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="结论可以"&gt;结论：可以！ &lt;a href="#%e7%bb%93%e8%ae%ba%e5%8f%af%e4%bb%a5" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;如果文件锁被另一个进程持有，尝试获取锁的进程会被阻塞。但内核只会将此进程的状态改为**可中断等待（interruptible sleep）&lt;strong&gt;状态，而非&lt;/strong&gt;不可中断等待（uninterruptible sleep）**状态。&lt;/p&gt;
&lt;p&gt;这意味着：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;没有信号到达时，进程处于睡眠状态，不参与调度&lt;/li&gt;
&lt;li&gt;一旦有信号到达且未被进程屏蔽，内核会立即将进程状态修改为**可运行（running）**状态，使进程重新参与调度&lt;/li&gt;
&lt;li&gt;信号处理函数得以执行&lt;/li&gt;
&lt;/ul&gt;
&lt;h2 id="实测信号打断锁等待"&gt;实测：信号打断锁等待 &lt;a href="#%e5%ae%9e%e6%b5%8b%e4%bf%a1%e5%8f%b7%e6%89%93%e6%96%ad%e9%94%81%e7%ad%89%e5%be%85" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;下面的 C 程序演示：一个进程持有锁，另一个进程阻塞在 &lt;code&gt;fcntl(F_SETLKW)&lt;/code&gt; 上，此时 &lt;code&gt;Ctrl+C&lt;/code&gt; 发送 &lt;code&gt;SIGINT&lt;/code&gt; 能否唤醒它。&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-c"&gt;#include &amp;lt;stdio.h&amp;gt;
#include &amp;lt;stdlib.h&amp;gt;
#include &amp;lt;unistd.h&amp;gt;
#include &amp;lt;fcntl.h&amp;gt;
#include &amp;lt;signal.h&amp;gt;
void handle_sigint(int sig) {
printf(&amp;quot;Received signal %d, exiting...\n&amp;quot;, sig);
exit(0);
}
int main() {
signal(SIGINT, handle_sigint);
int fd = open(&amp;quot;/tmp/test.lock&amp;quot;, O_RDWR | O_CREAT, 0644);
if (fd == -1) {
perror(&amp;quot;open&amp;quot;);
return 1;
}
struct flock fl = {
.l_type = F_WRLCK,
.l_whence = SEEK_SET,
.l_start = 0,
.l_len = 0, // 锁定整个文件
};
printf(&amp;quot;Trying to acquire lock...\n&amp;quot;);
fcntl(fd, F_SETLKW, &amp;amp;fl); // 阻塞等待锁
printf(&amp;quot;Lock acquired!\n&amp;quot;);
pause(); // 持有锁，等待测试
close(fd);
return 0;
}
&lt;/code&gt;&lt;/pre&gt;
&lt;pre&gt;&lt;code class="language-bash"&gt;# 终端 1：先启动一个实例持有锁
$ gcc -o main main.c &amp;amp;&amp;amp; ./main &amp;amp;
Trying to acquire lock...
Lock acquired!
# 终端 2：启动第二个实例，它会阻塞在 fcntl(F_SETLKW) 上
$ ./main &amp;amp;
Trying to acquire lock...
# &amp;lt;阻塞中，状态为 interruptible sleep&amp;gt;
# 按 Ctrl+C 发送 SIGINT
^CReceived signal 2, exiting...
&lt;/code&gt;&lt;/pre&gt;
&lt;p&gt;进程在获取不到文件锁时，内核只是将进程状态修改为&lt;strong&gt;可中断等待状态&lt;/strong&gt;，因此 &lt;code&gt;Ctrl+C&lt;/code&gt; 产生的 &lt;code&gt;SIGINT&lt;/code&gt; 能被正常接收和处理。&lt;/p&gt;</description></item></channel></rss>