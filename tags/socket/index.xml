<?xml version="1.0" encoding="utf-8" standalone="yes"?><rss version="2.0" xmlns:atom="http://www.w3.org/2005/Atom"><channel><title>Socket on</title><link>/tags/socket/</link><description>Recent content in Socket on</description><generator>Hugo -- gohugo.io</generator><language>en-US</language><lastBuildDate>Sun, 19 Jul 2026 01:00:30 +0800</lastBuildDate><atom:link href="/tags/socket/index.xml" rel="self" type="application/rss+xml"/><item><title>tcp状态机：SO_LINGER 精确控制 close 发送 FIN/RST</title><link>/blog/2026-07-19-tcpip_so_linger/</link><pubDate>Sun, 19 Jul 2026 01:00:30 +0800</pubDate><guid>/blog/2026-07-19-tcpip_so_linger/</guid><description>&lt;h1 id="背景"&gt;背景 &lt;a href="#%e8%83%8c%e6%99%af" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;p&gt;在 [TCP 连接终止场景分析] 里，我们梳理过几类「关连接到底发 FIN 还是 RST」的情况：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;进程被 &lt;code&gt;kill -9&lt;/code&gt; 后，内核代为关掉 socket：和主动/被动打开无关，默认往往走 &lt;strong&gt;FIN&lt;/strong&gt;；&lt;/li&gt;
&lt;li&gt;关闭瞬间&lt;strong&gt;接收缓冲区还有未读数据&lt;/strong&gt;时，内核倾向于发 &lt;strong&gt;RST&lt;/strong&gt;，因为这些数据注定被丢弃；&lt;/li&gt;
&lt;li&gt;应用层 &lt;code&gt;Write()&lt;/code&gt; 成功，也不代表对端一定还活着。&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;文中还顺带提到另一种会触发 RST 的路径：&lt;code&gt;SO_LINGER&lt;/code&gt; 设为「开启且等待时间为 0」。&lt;br&gt;
这和「缓冲里有脏数据」不同——它是&lt;strong&gt;应用主动选择&lt;/strong&gt;的关闭策略。本文单独把 &lt;code&gt;SO_LINGER&lt;/code&gt; 说清楚：它管的是什么、三种配置分别怎样、什么时候值得用、又会踩什么坑。&lt;/p&gt;
&lt;h1 id="正文"&gt;正文 &lt;a href="#%e6%ad%a3%e6%96%87" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h1&gt;
&lt;h2 id="1-close-默认在干什么"&gt;1. close() 默认在干什么？ &lt;a href="#1-close-%e9%bb%98%e8%ae%a4%e5%9c%a8%e5%b9%b2%e4%bb%80%e4%b9%88" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;对 TCP 而言，应用调用 &lt;code&gt;close(fd)&lt;/code&gt;（引用计数归零）后，内核大致会：&lt;/p&gt;
&lt;ol&gt;
&lt;li&gt;若接收缓冲里&lt;strong&gt;还有未读数据&lt;/strong&gt; → 常直接 &lt;strong&gt;RST&lt;/strong&gt;（与 linger 无关，见前述场景二）；&lt;/li&gt;
&lt;li&gt;否则进入 &lt;code&gt;tcp_close&lt;/code&gt; 一类路径，尽量把发送缓冲里的数据发完，再发 &lt;strong&gt;FIN&lt;/strong&gt;，进入正常挥手；&lt;/li&gt;
&lt;li&gt;在默认 linger 语义下，&lt;code&gt;close()&lt;/code&gt; &lt;strong&gt;不必等&lt;/strong&gt; 对端 ACK 完整个挥手过程——未发完的数据可交给内核在后台继续处理（socket 变成孤儿套接字等，细节见 [close 后 socket 是否立刻销毁]）。&lt;/li&gt;
&lt;/ol&gt;
&lt;p&gt;因此默认行为更接近：&lt;strong&gt;尽快把 fd 从进程里摘掉，优雅关闭尽量交给协议栈&lt;/strong&gt;。&lt;br&gt;
&lt;code&gt;SO_LINGER&lt;/code&gt; 则允许你改写第 2、3 步的语义——包括「要不要阻塞等发送结束」，以及「干脆别 FIN，直接 RST」。&lt;/p&gt;
&lt;h2 id="2-so_linger-是什么"&gt;2. SO_LINGER 是什么？ &lt;a href="#2-so_linger-%e6%98%af%e4%bb%80%e4%b9%88" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;&lt;code&gt;SO_LINGER&lt;/code&gt; 是 &lt;code&gt;SOL_SOCKET&lt;/code&gt; 层选项，用一个 &lt;code&gt;linger&lt;/code&gt; 结构描述关闭策略：&lt;/p&gt;</description></item><item><title>tcp状态机：close(fd)后socket真的销毁了吗</title><link>/blog/2026-07-18-tcpip_close%E5%90%8Esocket%E7%9C%9F%E7%9A%84%E9%94%80%E6%AF%81%E4%BA%86%E5%90%97/</link><pubDate>Sat, 18 Jul 2026 20:00:09 +0800</pubDate><guid>/blog/2026-07-18-tcpip_close%E5%90%8Esocket%E7%9C%9F%E7%9A%84%E9%94%80%E6%AF%81%E4%BA%86%E5%90%97/</guid><description>&lt;h2 id="问题背景"&gt;问题背景 &lt;a href="#%e9%97%ae%e9%a2%98%e8%83%8c%e6%99%af" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;在网络编程中，许多人存在一个直觉误区：&lt;strong&gt;认为应用层执行 &lt;code&gt;close(fd)&lt;/code&gt; 且引用计数归零后，内核的 socket 就立即销毁了，此时收到任何数据必然会立刻回复 RST。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;&lt;strong&gt;这个理解是错误的。&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;应用层销毁 &lt;code&gt;fd&lt;/code&gt;（钥匙）与内核销毁 &lt;code&gt;socket&lt;/code&gt; 状态机（实体）是&lt;strong&gt;异步解耦&lt;/strong&gt;的。调用 &lt;code&gt;close()&lt;/code&gt; 后，该套接字会变成孤儿套接字（Orphan Socket）由内核接管。对于后续到来的数据，内核有一套极其严密的防御和处理机制。&lt;/p&gt;
&lt;h2 id="前置知识shutdown-与-close-的本质区别"&gt;前置知识：shutdown() 与 close() 的本质区别 &lt;a href="#%e5%89%8d%e7%bd%ae%e7%9f%a5%e8%af%86shutdown-%e4%b8%8e-close-%e7%9a%84%e6%9c%ac%e8%b4%a8%e5%8c%ba%e5%88%ab" class="anchor" aria-hidden="true"&gt;#&lt;/a&gt;&lt;/h2&gt;
&lt;p&gt;在深入内核行为之前，必须先澄清一个容易混淆的问题：为什么 &lt;code&gt;shutdown()&lt;/code&gt; 不能替代 &lt;code&gt;close()&lt;/code&gt;？两者到底操作的是什么？&lt;/p&gt;
&lt;pre&gt;&lt;code class="language-text"&gt;应用层
│
├── fd (文件描述符) ─── close() 操作这一层
│
├── struct file ─── 引用计数为0时销毁的是这个，而非最底层的struct sock
│ ...
└── struct sock, socket (内核对象) ─── shutdown() 操作这一层 ─── TCP 状态机在这里
&lt;/code&gt;&lt;/pre&gt;
&lt;ul&gt;
&lt;li&gt;&lt;code&gt;shutdown()&lt;/code&gt; 无论 &lt;code&gt;how&lt;/code&gt; 取 &lt;code&gt;SHUT_RD&lt;/code&gt;、&lt;code&gt;SHUT_WR&lt;/code&gt; 还是 &lt;code&gt;SHUT_RDWR&lt;/code&gt;，都直接操作 &lt;code&gt;struct sock&lt;/code&gt; 层的半关闭标志，&lt;strong&gt;完全不动 &lt;code&gt;struct file&lt;/code&gt; 的引用计数 (&lt;code&gt;f_count&lt;/code&gt;)&lt;/strong&gt;。&lt;/li&gt;
&lt;li&gt;&lt;code&gt;close(fd)&lt;/code&gt;时，引用计数-1指的是对struct file引用计数-1。引用计数为0时，销毁的是 &lt;code&gt;struct file&lt;/code&gt;，而不是 &lt;code&gt;struct sock&lt;/code&gt;（socket传输控制块）。释放 &lt;code&gt;struct file&lt;/code&gt; 时通过 &lt;code&gt;file-&amp;gt;f_op-&amp;gt;release()&lt;/code&gt; → &lt;code&gt;sock_close()&lt;/code&gt; → &lt;code&gt;tcp_close()&lt;/code&gt; 驱动 TCP 状态机流转。&lt;code&gt;tcp_close()&lt;/code&gt; 只是驱动 TCP 状态机迁移到 &lt;code&gt;FIN_WAIT_1&lt;/code&gt; / &lt;code&gt;FIN_WAIT_2&lt;/code&gt;，&lt;code&gt;struct sock&lt;/code&gt; 本身并未直接销毁——它作为&amp;quot;孤儿套接字&amp;quot;继续由内核协议栈接管，直到对端关闭或超时。&lt;/li&gt;
&lt;/ul&gt;
&lt;table&gt;
&lt;thead&gt;
&lt;tr&gt;
&lt;th&gt;调用&lt;/th&gt;
&lt;th&gt;发 FIN？&lt;/th&gt;
&lt;th&gt;递减引用计数？&lt;/th&gt;
&lt;th&gt;fd 还能用？&lt;/th&gt;
&lt;/tr&gt;
&lt;/thead&gt;
&lt;tbody&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;shutdown(fd, SHUT_WR)&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;✅ 是&lt;/td&gt;
&lt;td&gt;❌ 否&lt;/td&gt;
&lt;td&gt;✅ 能&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;shutdown(fd, SHUT_RD)&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;❌ 否&lt;/td&gt;
&lt;td&gt;❌ 否&lt;/td&gt;
&lt;td&gt;✅ 能&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;shutdown(fd, SHUT_RDWR)&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;✅ 是&lt;/td&gt;
&lt;td&gt;❌ 否&lt;/td&gt;
&lt;td&gt;✅ 能&lt;/td&gt;
&lt;/tr&gt;
&lt;tr&gt;
&lt;td&gt;&lt;code&gt;close(fd)&lt;/code&gt;&lt;/td&gt;
&lt;td&gt;视情况？见下文&lt;/td&gt;
&lt;td&gt;✅ 是&lt;/td&gt;
&lt;td&gt;❌ 不能&lt;/td&gt;
&lt;/tr&gt;
&lt;/tbody&gt;
&lt;/table&gt;
&lt;p&gt;&lt;img src="https://upload.wikimedia.org/wikipedia/commons/thumb/f/f6/Tcp_state_diagram_fixed_new.svg/3840px-Tcp_state_diagram_fixed_new.svg.png" alt="tcp state diagram"&gt;&lt;/p&gt;</description></item><item><title>REUSEPORT vs REUSEADDR</title><link>/blog/2018-11-24-kernel-reuseport-vs-reuseaddr/</link><pubDate>Sat, 24 Nov 2018 10:17:22 +0800</pubDate><guid>/blog/2018-11-24-kernel-reuseport-vs-reuseaddr/</guid><description>&lt;p&gt;&lt;strong&gt;REUSEPORT vs REUSEADDR&lt;/strong&gt;&lt;/p&gt;
&lt;p&gt;这是我见过的最REUSEPORT和REUSEADDR最全面的解释和回答了，在stackover上1360赞同（包含我的一票）👍，简单总结下，详情还是建议想读以下内容，特别是不同操作系统的区别：&lt;/p&gt;
&lt;ul&gt;
&lt;li&gt;TCP/UDP连接（UDP无连接但可以connect），由五元组表示：&amp;lt;协议类型，源ip，源端口，目的ip，目的端口&amp;gt;；&lt;/li&gt;
&lt;li&gt;REUSEADDR解决的是监听本地任意地址0.0.0.0:port与另一个监听本地特定地址相同端口a.b.c.d:port的问题；&lt;/li&gt;
&lt;li&gt;REUSEPORT解决多个sockets（可能归属于相同或者不同的进程）是否允许bind到相同端口的问题，Linux下为了避免port hijack，只允许euid相同的进程bind到相同的port（bind设置socket源port，connect设置socket目的端口），同时对于tcp listen socket、udp socket还会进行“均匀的”流量分发，也是一个轻量的负载均衡方案。fuser&lt;/li&gt;
&lt;/ul&gt;
&lt;p&gt;ps: REUSEADDR还可以快速复用TIME_WAIT状态的地址(ipport)，比如服务器重启后重新listen同一个地址，但是单独这个选项不允许多个active listeners复用这个地址。&lt;/p&gt;
&lt;p&gt;原文出自：&lt;a href="https://stackoverflow.com/a/14388707/3817040"&gt;reuseport &amp;amp; reuseaddr, how they differ?&lt;/a&gt;&lt;/p&gt;
&lt;p&gt;Welcome to the wonderful world of portability&amp;hellip; or rather the lack of it. Before we start analyzing these two options in detail and take a deeper look how different operating systems handle them, it should be noted that the BSD socket implementation is the mother of all socket implementations. Basically all other systems copied the BSD socket implementation at some point in time (or at least its interfaces) and then started evolving it on their own. Of course the BSD socket implementation was evolved as well at the same time and thus systems that copied it later got features that were lacking in systems that copied it earlier. Understanding the BSD socket implementation is the key to understanding all other socket implementations, so you should read about it even if you don&amp;rsquo;t care to ever write code for a BSD system.&lt;/p&gt;</description></item></channel></rss>